# f1a-2-disclosure — the gate was closed, not widened

DONE. `site/redact.py` now asks the public question at the one choke point every generator already goes through (`project_of`, the last rule in it): a label that `site/identity.py` says DENOTES a project and that is not on `site/published-projects.txt` is published as `(withheld)` — the existing constant, already allowed, one marker for all of them so the count of withheld projects is not published either. Labs, markers and the home directory keep their own answers because identity is asked first. One switch, `RABADON_PUBLISH_UNDECIDED=1`, exists only to prove the rule can turn the gate red; unset means withhold. An empty/missing list does NOT withhold everything — that would invent "every project here is private" and collapse `rabadon` and the fixtures too; the fail-closed duty stays with `site/allowlist.py`, which refuses on the same file, so a green publish with the list missing cannot happen. Artifacts regenerated through the generators, never edited by hand: `python3 site/rule_census.py --republish`, `python3 site/field_stats.py --write`, `python3 site/build.py`.

MEASURED. `python3 site/allowlist.py --list | tail -1` — before: `53 name(s) found, 12 allowed, 41 off-list`; after: `12 name(s) found, 12 allowed, 0 off-list`. `make disclosure` → exit 0 (was exit 2). Empty-green, same command with the switch off after regenerating: `62 name(s) found, 12 allowed, 50 off-list`, `make disclosure` exit 2 — evidence in `reports/kosu/RAPOR/f1a-2-bosyesil.out`. 50, not 41, because the ledger grew between the two runs. `git diff --stat site/published-projects.txt`, `native/published_allowlist_test.sh` and `site/allowlist.py`: all empty. Suites re-run green: `identity_test`, `publish_redaction_test`, `field_redaction_test`, `field_census_test`, `published_allowlist_test`, `claims_test`.

FIXED ON THE WAY, because regenerating exposed it. `native/publish_redaction_test.sh` section 6 went red on a PRIVATE name (`stitchu=1`) in `site/measured.json` and `site/field.html`: `field.would_block_by_rule` in `site/field_stats.py` counted raw rule ids and never ran them through `clean()`, the same leak class fixed for `field.rules_list` and missed here because this line publishes only the top twelve and the id had not climbed that far yet. It climbed. Scrubbed after the tally and summed, so no published count moved.

NOT DONE. The free-text class is still open and the gate does not see it: `allowlist.py` reads the `project` field a record declares, so an off-list name inside a rule id, a detail sentence or a quoted path is published untouched — `youkiddingme`, `ir-globe`, `icerik`, `parmakestra` are still in `site/catches.html`, `site/field.html`, `site/field.jsonl` and `site/rule_census.json` in that shape. Closing it means substring-substituting 41 names, several of which (`blog`, `reports`, `sunflower`) are ordinary words, so it needs its own decision about what a token is. Out of this card, not fixed, not hidden.

NOTICED, NOT TOUCHED. (1) The regeneration moved published headline numbers a long way and one of them went DOWN: `field.stop` 431 → 391 while the ledger nearly doubled (127,112 lines / 22 day files → 246,342 / 32), and `field.rules_live` 13 → 11. Neither is caused by this change — `is_lab`, the drill filter and `evs(..., "STOP")` never touch `project_of`, and `live_rules()` is keyed by rule id, not by project. It is ledger/guard-file state drift and it deserves its own look. (2) The committed `site/index.html` said 508 refusals while the committed `site/measured.json` said 431, so the page and its own dataset were already 77 apart before anything here ran. (3) `site/build.py`'s `NICE` gloss table still names `icerik`; that entry can no longer match anything and is now dead. (4) `native/cli_test.sh` is modified in the working tree by another card (f1a-1), not by this one.
