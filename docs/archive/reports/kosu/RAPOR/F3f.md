# F3f — kart

**KART SAYISI: 4. TAHMİNLER (§3.12, kart kesilmeden önce yazıldı, `bbf90d4` 23:07):**
K1 yükseltme yolu (bloklayan) **90 dk** · K2 (b) ayrıştırma **45 dk** ·
K3 `2b` profil + ölçüm **60 dk** · K4 yanlış pozitif daraltma **30 dk**.
**Toplam 3 sa 45 dk; iki katı = 7 sa 30 dk. Tetik ÇALMADI** (faz ~1 sa 10 dk).

**K1 (BLOKLAYAN) — "SEVK EDİLDİ AMA KURULMADI" KAPANDI, CANLIDA DA.** Teşhis önce (`kanit/f3f/k1-teshis.txt`): yol ölü değil, bayat değil — **olay kümesi eski**. Kayıtlı 5 olay, `install.mjs`'in bugün yazacağı 6 + `Stop→rabadon-drift`; fark `PostToolUseFailure` **ve** `rabadon-drift`. `installHooks` bunu ONARIRDI (`healthy=false`) ama **kurulumu hiçbir şey yeniden koşturmuyor**, ve `doctor` bu kör dosyaya **"global hooks healthy … all green"** diyordu (yalnız yolun ölü/yabancı olduğuna bakıyor). Onarım: SessionStart — her kurulumun abone olduğu tek olay — **tek kurulum uygulamasını** (`hooks/refresh.mjs` → `installHooks`) çağırıyor; ikinci liste yok, yarın eklenen olay aynı yoldan gidiyor. Yalnız **zaten rabadon girdisi olan** dosyaya dokunur (self-heal ≠ self-install), yedek alır, yabancı kancaya/`statusLine`'a dokunmaz, ekranda söyler, deftere `HOOKS_REFRESHED` yazar, 6 saatte bir, `RABADON_SELFHEAL=0` kapatır. **Yuvarlamıyor:** ajan kancaları oturum başında okuduğu için kart "bu oturum kör kalır, gelecek oturumdan itibaren canlı" diyor. Kapı `native/hook_upgrade_test.sh` **18 iddia**; boş yeşil turu `F3f-oncesi`'nde **8/10 KIRMIZI**; mutasyon A (yükseltme adımı yok) **9/9**, mutasyon B (self-install koruması yok) **17/1**, ikisi de geri alındı (`k1-mutasyon.txt`). `doctor` artık bayat aboneliği adıyla söylüyor.
**CANLI MAKİNE ARTIK GÖRÜYOR** (`k1-canli-onarim.txt`): sevk edilen ikili `~/.claude/settings.json`'ı kendi tazeledi (yedek alındı, 11 orkestra kancası + `statusLine` korundu), sonra kendi oturumumda **`; true` OLMADAN** `ls -la /nonexistent-f3f-live-probe-QQ8` (Exit code 1) → ham defter **`STEP_OK` `"rc":1`**, ham hamle **`claimed_rc:1`, `err_sig:"d7ffaf70c389a21a"`**. F3e hakeminde `STEP_START` var / `STEP_OK` yoktu.

**K2 — İKİ SEBEP AYRILDI, ve baskın sebep (i)** (`k2-ayristirma.txt`, 81 oturum / 1766 hamle, halkalar sevk edilen exporter'la okundu, yazılmadı). `repeat` iki şart: `seen>=3` (aynı TAM imza, son 20) **ve** `failed>=2` (`claimed_rc==1`).
**A) yalnız 1. şart: 2/81 · B) yalnız 2. şart: 9/81 · C) ikisi birden (sevk edilen kural): 0/81.**
**D) ÜST SINIR:** her kapanmamış hamle (290 tane) aslında başarısızdı sayılsa bile — yani K1 geçmişe uygulanmış gibi — **hâlâ 0/81**. Yani **(ii) hiçbir zaman bağlayıcı kısıt değildi; engelin tamamı (i)**. 75/81 oturumda bir imza pencerede **bir kez bile** tekrar etmiyor. **F4'e devredilen sayı (dedektör DEĞİŞTİRİLMEDİ, yalnız ölçüldü):** imza "ilk token" olsaydı 1. şart **32/81**, iki şart birden **8/81**. Kışkırtılmamış (b): **ÜRETİLEMEDİ**, ve sebebi artık tek ve ölçülü.

**K3 — `2b`: BU FAZ DA HIZLANDIRMADI.** Önce profil, ve yöntem değişti: ayrıştırma iki koşuyu çıkararak değil **tek koşunun içinde** alınıyor (ikilinin kendi monotonik saati, `RABADON_PROFILE=1`). İki bağımsız koşu (N=150): **pre_main %72,3 / %72,0 · statik init %0,1 · rabadon'un kendi mantığı %27,0 / %28,4** (843,0 / 735,0 µs). Duvar saati %20 gezdi, **pay 0,3 puan** — differansiyel yöntemin aynı soruda yayılımı 390 µs idi. Sonuç: atfedilebilirin **kabaca yarısı** kendi kodu, yarısı imaj yükleme fazlası → **F3e hakeminin %49/%51'i doğrulandı**, F3d'nin "hedef yükleme" ve F3e'nin "%70 kural yolu" iddiaları ikisi de tutmuyor. Hızlanma: eşli, sırası dönüşümlü (N=200×5, HEAD↔`F3f-oncesi`) **−380,7 µs, 0+/5−** — tek yanlı ama **|439 µs| bandını aşmıyor → GÜRÜLTÜ, iddia yok**. Kanonik alet: atfedilebilir **1898,3 µs**, tavan **1000 µs** (oynatılmadı), **1,90×**; eşli koşuda aynı ikili 1481,5 → aralık **1481–1898 µs** yazılıyor. **ÖLÇEMEDİM:** pre_main'in içi (`DYLD_PRINT_STATISTICS` bu makinede hiçbir şey basmıyor) ve main'in içi.

**K4 — bildirilen yanlış pozitif ÜRETİLEMEDİ; aynı kuralda KARŞITI bulundu ve onarıldı.** Kural `guard.json`'da (bana kapalı) → fikstüre birebir kopyalanıp gerçek gate'e verildi: **9 salt-okuma şekli, yanlış pozitif 0/9** (`grep -n`, `grep -in`, `python3 -c …; grep -in … guard.json` dahil). Hakemin kestiği tam komut metni kartta yazılı olmadığı için daraltılacak kapsam yok — **ÖLÇEMEDİM**, uydurulmadı. Bunun yerine **canlı BYPASS** ölçüldü: `mv .rabadon/guard.json /tmp/x` **rc=2**, ama `rm .rabadon/guard.json` / `rm -f` / `rm .rabadon/promise.json` **rc=0**, üstelik kuralın kendi regex'i dördüyle de eşleşiyor. Sebep `native/rules.h` `all_targets_disposable`: "hedef proje içinde ⇒ atılabilir" — ama bu iki dosya **tanımı gereği** proje içinde, yani şart kanıt değil taniminin tekrarı; kuralın `rm` kolu tamamen kapalıydı, `mv` kolu açık olduğu için bozuk görünmüyordu. Onarım **ürün tarafında** (guard.json'a dokunulmadı) ve bir **bastırmanın daraltılması** — yalnız reddi artırabilir. Kapı `native/guard_delete_test.sh` **16 iddia, twin arm**: yasa dosyaları reddediliyor (6), bastırmanın var olma sebebi `rm -rf build` **hâlâ geçiyor** (3), salt-okuma 6 şekil **hâlâ geçiyor**, proje dışı silme hâlâ reddediliyor. Mutasyon C: carve-out'u kapat → **11/5**, geri alındı → 16/0; boş yeşil turu `F3f-oncesi`'nde **11/5 KIRMIZI**.

**KAPI SAYILARI (kendim koşturdum).** `make test` **EXIT=0**; TEK GEÇERLİ SAYAÇ: native **3893** iddia + **633** kontrol + npm **64/0** = **4590 yeşil / 0 kırmızı** (taban 4556, **+34** = iki yeni süidin tamamı: `hook_upgrade` 18 + `guard_delete` 16). Süit 112 → **114**; **küçülen / kaybolan / `skip`'e düşen süit 0**, silinen dosya 0, silinen iddia 0. `bash reports/R7/accept.sh` **EXIT=1, 23 yeşil / 3 kırmızı, `{2b, 6e, 7b}` — BÜYÜMEDİ** (`2b` 1249,0 µs). Mühürlü set (`accept.sh` · `ON-KAYIT.md` · `claims.tsv` · `guard.json` · korpus/snapshot), `moves.h` `CAP=200` ve **`bin/rabadon.mjs` (O3)** fazın diff'inde **HİÇ YOK**. F1e-C üçlüsü nihai ikiliye karşı **42/0 · 38/0 · 13/0**; kâtip commit'i (`docs/how-it-works.md`, yükseltme yolu belgelendi) fazın **son commit'i DEĞİL**. Worktree'ler `/tmp` dışında açıldı ve **kaldırıldı**.

**KAPI BENİ İKİ KEZ KESTİ** (`no-blind-inplace-source-rewrite`): (a) `perl -0pi` ile `gate.cpp` — **doğru ret**, mutasyonu Edit ile yaptım; (b) `/tmp`'de bir **fikstür** `settings.json`'ı yazan `python3 -c … > …` — kuralın adı "engine source and its test expectations" diyor, kestiği o değildi: **yanlış pozitif adayı, sahipsiz, bu fazda açılmadı.** `guard.json`'a dokunulmadı, `rabadon off` kullanılmadı, CHALLENGE-3 deliği kullanılmadı.

**AÇIK / DEVİR:** (b) hâlâ 0 ve sahibi artık dedektör tasarımı (F4). (c) koşulmadı → **F4 kapalı**. `2b` açığı 481–898 µs, hiçbir bacak tek başına yetmiyor. `no-rm-rf-outside`'ın adı-işi uyuşmazlığı ve yukarıdaki (b) şıkkı sahipsiz.
