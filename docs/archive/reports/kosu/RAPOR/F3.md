# F3 — iki bloklayan kart kesildi, üçüncüsü AÇILMADI

Taban `F3-oncesi` = `6913ae1`. `main`, 4 commit. Kanıt: `reports/kosu/kanit/f3/`.

**KART 1 · D6 — GEÇTİ.** Fikstür koddan ÖNCE, ayrı commit (`6350d8d` → `d4e80e8`). `native/discovery_scope_test.sh`: faz öncesi ikilide **7 ok / 6 fail**, sonrasında **13/0**. Hakemin iki hücresi de tuttu: `.venv/.../site-packages` **level 1 kaldı**, `Library/Python/3.9/.../site-packages` **level 3 → 1 düştü**. `Library` adı `skip_dir`'e **EKLENMEDİ** — ölçtüm, eklemek `src/Library/` projesini kör ediyor; yerine `site-packages` + `dist-packages` eklendi (`repair.cpp:487`/`classify.h:82` zaten taşıyordu, ayrışan liste `truth.cpp`'ti). `pathres.h project_root()` artık **$HOME'u kök seçmiyor**, altındaki gerçek git kökü hâlâ kazanıyor. `net.cpp` boş-koşu muafiyeti **pytest exit 5**'e genişledi — `emptyRun` tek başına değil `(rc==0||rc==5)`, çünkü import hatası `collected 0 items` basıp 2 ile çıkar ve onu muaf tutmak yanlış reddi kaçırılan yakalamayla takas ederdi.
Mutasyon: üç mutant ayrı ayrı **10/3 · 11/2 · 12/1** (`f3-mutasyon.out`). Boş yeşil: `F3-oncesi` artefaktında ayrı worktree'de **7/6 KIRMIZI**.

**KART 2 · CHALLENGE-2 — YENİDEN ÜRETİLDİ, sonra onarıldı.** Sevk edilen ikilide, kum havuzunda: P kırmızıyken hook cwd=P iken `cd <komşu> && git commit` **exit 2**, hook cwd=komşu iken aynı komut **exit 0** — ret, eylemin dokunduğuna değil oturumun nerede başladığına bakıyordu. `native/redbase_scope_test.sh`: faz öncesi **8 ok / 1 fail**, sonrasında **9/0**. Muafiyet kasten dar: her segment kırmızı ağacın dışında olmalı, `cd`/`pushd`/`popd` kabuk hareketidir sayılmaz, `git -C` işaret ettiği depoya izlenir, ayrıştırılamayan satıra muafiyet YOK; `cd uzağa && cd geri && git commit` hâlâ **exit 2**. Bypass ikizi gerçek kapıdır: `git -C` takibi kapatılınca **8/1**.

**KART 3 · F3-S1 — BAŞLAMADI, tabanı bağımsız yeniden ölçüldü.** `kanit/f3/2b-uctan-uca.sh` (yeni, yalnız ölçer): `F3-oncesi` **1985,7 µs = 1,99×**, nihai ikili **1941,3 µs = 1,94×**. §8.5'in 1,99×'i üçüncü harness'ta doğrulandı; **tavan 1000 µs oynatılmadı**, sıcak yol yavaşlamadı (kart 2'nin ayrıştırması yalnız `netRed` dalında koşar). Onarım YAPILMADI. **§3.12 gereği duruyorum**: KOSU §F3'ün kendi kapsamı (canlı enjeksiyon, merdiven, (b)/(c)) iki bloklayan kartın üstüne konursa tahminin iki katını aşar. Sıradaki kartı hakem yazsın.

**KAPI SAYILARI, kendim koşturdum.** `make test` **EXIT=0**, native **3808** iddia + **633** kontrol, `npm test` **64/0** → **4505 yeşil / 0 kırmızı** (taban 4483, **+22**). `bash reports/R7/accept.sh` → exit 1, **23 yeşil / 3 kırmızı**, **`{2b, 6e, 7b}` BÜYÜMEDİ** (`2b` 1240,2 µs). Silinen/zayıflatılan/atlanan test YOK; eşik/tolerans/fikstür/ön-kayıt HİÇ değişmedi; `reports/R7/accept.sh`, `ON-KAYIT.md`, `~/.rabadon/guard.json` bu fazda AÇILMADI. `Makefile`'a yalnız iki süit satırı eklendi.

**CHALLENGE-3 KULLANILMADI.** Kapı beni bir kez gerçekten kesti (`no-blind-inplace-source-rewrite`, `perl -0pi` ile mutasyon kurma denemesi) — doğru ret, mutasyonları Edit ile yeniden kurdum. `rabadon off` koşmadım, `guard.json`'a dokunmadım, bileşik-komut deliğini kullanmadım.

**ÖLÇEMEDİM / DOĞRULANMADI.**
1. Canlı `$HOME` üstünde site-packages onarımının etkisi **ölçülemez**: iki ikili de `discoveryCapped:["depth","budget"]` veriyor, yani 20657 → 20672 farkı ağacın değil **sınırın** şekli (Kapı 2), ve `via:` hâlâ elle daraltılmış `guard.json check`. Bütün D6 ölçümleri bu yüzden fikstürle üretildi.
2. `rabadon-truth $HOME` doğrudan çağrılınca hâlâ level 3 / 20672 dosya — `project_root()` onarımı buna dokunmaz, orada kökü kullanıcı açıkça veriyor.
3. Temiz konteynerde hiçbir şey koşmadı; iki yeni süit yalnız macOS'ta ölçüldü.
4. **`make` başlık bağımlılığı izlemiyor:** `pathres.h` düzenlenip `make all` koşulunca ikili YENİDEN DERLENMEDİ, mutant 2 sahte yeşil verdi (`touch` ile yakalandı). Bayat ikiliyle yeşil üretilebilir. Kart açılmadı.
5. Kart 2'nin muafiyeti `git -C`'yi izliyor; `--git-dir` / `GIT_DIR=` için ayrı arm yazmadım.
6. **Ölçülmüş yanlış pozitif: 1.** `make test` çıktısını `grep -nE "FAIL"` ile taradığım komuta PostToolUse "tests are RED" bastı; `make test` EXIT=0'dı, kural benim grep çıktımı süit çıktısı sandı. Sayılıyor, mazur görülmüyor.
