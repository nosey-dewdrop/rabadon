# F3g

**KART SAYISI: 3. TAHMİNLER (kart kesilmeden yazıldı, §3.12, commit `ce199e9`):**
K1 yasa ailesi **90 dk** · K2 yanlış pozitif **45 dk** · K3 `2b` profil **45 dk**. İki katı aşılmadı.

**K1 — SEKİZ ŞEKLİN SEKİZİ DE KAPANDI, ve şekil değil aile kapatıldı.** F3f'in bıraktığı hâl `/tmp` DIŞINDA, `$HOME` altında bir proje kum havuzunda ölçüldü (`kanit/f3g/k1-oncesi.txt`): `rm -rf .rabadon` · `rm -rf ./.rabadon` · `truncate -s 0` · `cp /dev/null` · `chmod 000` · `ln -sf` · `install` · `dd of=` · `find -delete` → **dokuzu da rc=0 ALLOW**. Bugün (`k1-sonrasi.txt`) **dokuzu da REFUSE**. Onarım verb saymıyor: `baseline-law-unmade` **eylemi** soruyor — bu segmentten sonra yasa hâlâ yerinde mi, bütün mü, okunabilir mi. Bilinmeyen bir verb yasaya doğrultulduğunda **kapalı** düşüyor (ikilideki tek yer; öznesi tam üç dosya adı). Kilit `native/law_family_test.sh` **68 iddia**: 9 ölçülen şekil + 27 kardeş (`rmdir`, `mv`, `unlink`, `shred`, `tee`, `sed -i`, `perl -pi`, `git rm`, `>>`, `chown`, `cd .rabadon && rm guard.json`, `find . -name guard.json -delete`, …) + 19 okuma + 12 sıradan yıkıcı iş. Fikstür `$HOME` altında açılıyor, `/tmp` değil.

**K2 — YANLIŞ POZİTİFİ ÖNCE KENDİM ÜRETTİM, sonra onardım.** HEAD'de `grep -c rm .rabadon/guard.json` → **rc=2**. Kural GEVŞETİLMEDİ, `guard.json`'a DOKUNULMADI (kapalı dosya, diff'te yok): daraltma ürün tarafında — *hiçbir şey yazmayan* bir segment bir yol kuralının öznesi olamaz (`rbbase::segment_writes_nothing`). Liste kasten cimri, **yorumlayıcı içermiyor**: `python3 x.py` ne yazacağı bilinmez ve onu geçirmek `no-blind-inplace-source-rewrite`'ı sessizce öldürürdü. Twin-arm tek süitte: `guard_delete_test.sh` **16 → 22**, ARM 1 gerçek ihlaller BLOCK, ARM 3 okumalar ALLOW, canlı kurala karşı.

**YANLIŞ POZİTİF SAYACI +1, ve onu kendi yasam üretti (§4.3).** `cd <...>/.rabadon` — hiçbir şey açmayan, yaratmayan, kısaltmayan bir komut — `baseline-law-unmade` ile reddedildi, bu kartı yazan oturumun kendi elinde. Bildirim değil, **5 iddia** olarak süite yazıldı (`3b4e85b`, koddan AYRI ve ÖNCE), kırmızı düştüğü görüldü, sonra `cd/pushd/popd/test/[` okuyucu listesine girdi. Kapalı-düşen listenin ilk faturası, açıkta.

**K3 — `2b`: PROFİL ÇIKTI, İDDİA YOK.** Tek koşunun İÇİNDE eşli bölünmüş, N=200, üç bağımsız koşu (`kanit/f3g/2b-bulgu.md`): **leg 1 exec+dyld+imaj 2312/2283/2307 µs (%70)** · leg 2 statik init 2,0 µs · **leg 3 rabadon'un kendi kodu 993/999/984 µs (%30)**. Yayılım 30 ve 16 µs — bandın (|439 µs|) çok altında, çünkü bacaklar iki popülasyonun farkı değil. **13 deny kuralının TAMAMI ölçüldü: 156,5 µs = duvarın %4,7'si** (leg 3, 13 kuralla 1016,0 vs 0 kuralla 859,5). **F3e'nin "%70 kural yolu" iddiası sayıyla çürüdü.** Aritmetik: atfedilebilir ≈ 1965 µs, kapatılması gereken 965 µs; **rabadon'un kendi kodunun tamamı silinse bile** kalan ≈ 975 µs, çünkü geri kalanı 818 KB'lık imajın yüklenmesi. **BU FAZ DA HIZLANDIRMADI** ve denemedi. `accept.sh` bugün üç kez koştu: 1297,5 / 1331,3 / 1691,2 µs — aletin kendi bugünkü yayılımı 394 µs, bandın içinde, tek yanlı değil. Tavan oynatılmadı, `accept.sh` diff'te YOK.

**MUTASYON KANITI, üç mutant** (`kanit/f3g/mutasyon.md`): yasa kapatıldı → 26/36 KIRMIZI; **yasa DOSYA adıyla korunup DİZİN kolu kapatıldı (= F3f'in sevk ettiği hâl) → 54/8 KIRMIZI**; K2 daraltması geri alındı → 18/4 KIRMIZI. Üçü de geri alınınca yeşil. **BOŞ YEŞİL TURU:** iki süit de `F3g-oncesi` (`906b1e1`) artefaktının üstünde, `--detach` worktree'de, `/tmp` DIŞINDA → **26/36 ve 18/4, EXIT=1** (`bosyesil.out`). Worktree kaldırıldı.

**KAPI:** `make test` **EXIT=0** · native iddia **3967** (taban 3893, **+74**: `law_family` 68 yeni + `guard_delete` +6) · `PASS (N checks)` **633** (değişmedi) · `npm test` **64/0** → **TOPLAM 4664 yeşil / 0 kırmızı** (taban 4590). **Süit 114 → 115. Küçülen süit 0, kaybolan 0** (bölüm bölüm karşılaştırıldı). `accept.sh` EXIT=1, **kırmızı ad kümesi `{2b, 6e, 7b}` — BÜYÜMEDİ**. F1e-C üçlüsü nihai ikiliye karşı **42/0 · 38/0 · 13/0**; kâtip commit'i (`ef58402`, `docs/guard.md`) fazın SONUNCUSU değil. Mühürlü set (`accept.sh` · `ON-KAYIT.md` · `docs/claims.tsv` · `.rabadon/guard.json`) ve `bin/rabadon.mjs` (O3) diff'te **HİÇ YOK**. Silinen/zayıflatılan/atlanan test **0**, `CAP=200` yerinde.

**KAPI BENİ KESTİ, iki kez, ikisi de DOĞRU RET.** (1) `rm -rf "$HOME/…/_f3g_kum"` → `no-rm-rf-outside`; (2) aynı silme relatif yolla → `baseline-rm-rf-outside`. Aşmadım. Kum havuzunu elle söktüm ama **`_f3g_kum/proj/.rabadon/guard.json` + `promise.json` DURUYOR**: onları silmeyi **bu kartın kendi yasası** reddediyor — yasa, kendi yazarının yasayı silmesini engelledi. Kaldırmak isteyen tek satır: `RABADON_OFF=1 rm -rf ~/damla_projects_2026/_f3g_kum`. Worktree ve `_f3g_bosyesil` kaldırıldı.

**ÖLÇEMEDİM / NOT VERIFIED.** Konteynerde hiçbir şey koşmadı (yalnız konak macOS). `law_family_test.sh` kum havuzunu `$HOME` altına açıyor; `$HOME` yazılamayan bir referans ortamında süit `exit 1` verir, bu ölçülmedi. Yasa yakalamıyor ve bu kasten yazıldı: yasayı **hecelemeyen** bir glob (`rm -rf .r*`), önceden kurulmuş bir symlink, ve shell OLMAYAN bir yorumlayıcıya verilen gövde (`python3 -c "shutil.rmtree(...)"`) — ayrıştırıcı shell'in `-c` dizgesini yeniden okuyor, python'unkini okumuyor. Ağacın tamamını silmek ayrı bir ailedir ve özyinelemeli-silme yasalarınındır. `2b`'nin yükleme bacağı ölçüldü ama **denenmedi** (bağlama/boyut ve süreç-başlatmama yolları bu kartın kapsamı değil). (c) negatif kontrolü kapsam dışıydı, koşulmadı → **F4 hâlâ kapalı**.
