# F2 — FAZ TUTANAĞI

Kök `/Users/damummyphus/damla_projects_2026/rabadon`, dal `main`.
Faz aralığı **`f03320f..HEAD`**. Yeni dal açılmadı; worktree yalnız alet olarak
(`/tmp`, `--detach`) kullanıldı ve kaldırıldı.
Kapsam: `SAPMA-KARARLARI.md` sonundaki **S1–S15** bloğu. Genişletilmedi.

Kart raporları tek tek: `f2-0-kart.md`, `f2-1-kart.md`, `f2-2-kart.md`,
`f2-3-kart.md` (+`f2-3-etiketleme.md`), `f2-4-kart.md`, `f2-5-kart.md`.
Bu dosya onların üstünde durur; sayı çelişirse **bu dosyadaki ölçüm günceldir**
(hepsi faz kapanışında, nihai ikiliye karşı yeniden koşuldu).

---

## 1. KAPANAN KARTLAR VE HER BİRİNİN PROOF KOMUTU

| kart | kapsam | commit(ler) | PROOF komutu | verbatim çıktı |
|---|---|---|---|---|
| **0 (BLOKLAYAN)** | S12 + S13 | `eeedfe4` (fikstür) → `2097d75` (onarım) → `49fd73d` (sayaç) | `./native/heredoc_prose_test.sh` · `bash native/refusal_rate.sh 286fd71d` | `f2-0-bosyesil.out`, `f2-0-heredoc-after.out`, `f2-0-refusal-rate.out`, `f2-0-hukum.md` |
| 1 | S5 export onarımı | `827baa7` (ölçüt) → `96f493a` (onarım) → `6df1953` (rapor) | `./native/moves_test.sh` | `f2-1-once.out`, `f2-1-sonra.out`, `f2-1-bosyesil.out`, `f2-1-kapi.out` |
| 2 | `usage --signals` ekranı (S1,S4,S6,S7) | `943f73c` (süit) → `28340e2` (ekran) → `c7b229c` (kanıt) | `./native/signals_screen_test.sh` · `rabadon usage --signals` | `f2-2-bosyesil.out`, `f2-2-ekran.out`, `f2-2-ekran-snapshot.out`, `f2-2-adim.out`, `f2-2-readonly.out` |
| 3 | etiketleme + ölçüm (S2,S8) | `9cba3cd` (R7 8a) → `9e81ac6` (ölçüm) | `python3 reports/kosu/RAPOR/f2-3-olcum.py` | `f2-3-olcum.out`, `f2-3-etiketleme.md`, `f2-3-r7accept.out` |
| 4 | S14 kardinalite | `30fdc67` (ölçüt, kırmızı) → `c8a2ad6` (`--silencers`) → `74e02fd` (kanıt) | `./native/docs_truth_test.sh` | `f2-4-bosyesil.out`, `f2-4-docstruth.out` |
| 5 | S15 referans ortam | `b93f3bf` (harness) → `c629065`, `8427fdc` (ham) → `760c349` (kart) | `bash native/refenv/run.sh --platform linux/amd64 …` / `--user 1000:1000` | `reports/refenv/f2-5-amd64-root-*`, `f2-5-arm64-nonroot-*` |
| **6 (kâtip, bu ajan)** | sevk edilen `--signals` yüzeyinin belgesi + S10/S11 kapanışı | `03c2da1` (ölçüt, kırmızı) → `6a6b03a` (belge) | `./native/signals_screen_test.sh` | `f2-6-kirmizi-once.out`, `f2-6-yesil-sonra.out` |

Hijyen: `fed3ed7` — `RABADON-ORKESTRASYON-v6.md` kökte **untracked** duruyordu
(koşunun kendi yönetişim belgesi sahipsizdi). İçeriğine dokunulmadan commit edildi.

Her kartta **kabul ölçütü, onu sağlayan koddan AYRI ve ÖNCEKİ commit'te**
(CLAUDE.md 2). `.rabadon/guard.json`, eşikler, `MIN_HISTORY`, %50, 1000 µs tavanı
**hiç oynatılmadı**.

---

## 2. S1–S15, TEK TEK

- **S1** — `n=0` sinyal `NOT MEASURED (n=0)` + sıfırın SEBEBİ basıyor, canlıya
  çıkmıyor. Dondurulmuş korpusta beş dedektörün **dördü** böyle:
  `repeat`, `oscillation`, `root_migration`, `green_redefined`
  (`f2-2-ekran-snapshot.out` satır 13–30).
- **S2** — `scope_drift` **n=17**, etiketlenen **0**; oran değil ham sayı basılıyor.
  Elle etiketleme `f2-3-etiketleme.md`: **17/17 YANLIŞ POZİTİF**. Hiçbir sinyal
  canlıya çıkmadı.
- **S3** — `make disclosure` exit 0 (F1a'da kapandı, bu fazda dokunulmadı).
- **S4** — okunan korpus VE kayıp ekranda: `34 session file(s) · 527 move(s) on
  disk`, `LOSS: 127` ve kaybı üreten ring **adıyla** (`286fd71d…`, 327 sayıldı /
  200 kaldı).
- **S5** — `native/audit.cpp` `jesc()` ile RFC 8259 kaçışlama. Ölçüm:
  **281/608 satır ayrışmıyordu → 527/527 ayrışıyor, 0 bayt kayıp**;
  `moves_test.sh`'in sessiz `except: continue` yutucusu artık **sayıyor ve
  adlandırıyor** (CLAIM 8), 21 → 22 iddia.
- **S6** — özet cümlesi 4 `NOT MEASURED` satırını da, korpusun kısalığını da, 127
  kaybı da açıkça yazıyor (ekranın son paragrafı). Süit gizlemeyi kırmızı düşürüyor.
- **S7** — ekranda "yakaladı / kurtardı / kazandırdı" yok; süit dokuz kelimeyi
  (`would have`, `saved`, `prevented`, `averted`, `rescued`, `avoided`, `sub-ms`,
  `we caught`, `would-have`) yasaklıyor (`signals_screen_test.sh:183`).
- **S8** — **kabul sayısı DONDURULMUŞ YEDEKTEN alındı:**
  `~/.rabadon-korpus-snapshot-20260826/` (34 oturum / 527 hamle / başlık 654 /
  127 kayıp). Canlı korpus **yalnız bilgi olarak** ayrı basıldı
  (`f2-2-ekran.out`: LOSS 1.417). **Hangisinin okunduğu bu satırda yazılıdır.**
- **S9** — aşağıda §4.
- **S10** — aşağıda §5.
- **S11 / F1e-C** — aşağıda §6.
- **S12** — kart 0. Beş tırnaklı şekil (`printf` çift/tek tırnak,
  `echo … >> dosya`, `python3 -c`, `git commit -m`) faz öncesi ikilide **BLOCK**
  olarak yeniden üretildi (`f2-0-bosyesil.out`), onarımdan sonra **ALLOW**.
  Çıplak `make test | grep -c ok ; echo exit=$?` ve mevcut yedi pozitif **BLOCK
  kaldı**. **(d) hücresi CHALLENGE'a gerek kalmadan yeşil:**
  `bash -c "<gerçek boru hattı>"` ölçüldü ve **BLOCK** — tırnaklı dizge bir
  kabuğa verildiğinde program sayılıyor (`Parsed::lines` vektörü). Süit
  14 → **21** iddia; tek iddia silinmedi.
- **S13** — `native/refusal_rate.sh <sid-öneki>` (bash + grep/sed/awk; python3,
  jq, node, ağ yok). **İki payda tanımı da ayrı basılıyor:** `STOP+BLOCKED` **16**,
  `WOULD_BLOCK` **24**, toplam **40**. **16 retin 16'sı tek tek hükme bağlandı**
  (`f2-0-hukum.md`): **DOĞRU 6 / YANLIŞ 6 / HÜKÜMSÜZ 4**. Hükümsüz kalan 4'ün
  sebebi ölçüldü ve ilan edildi: defterin `STOP.detail` alanı komutu **160 bayt**ta
  kesiyor. İki yeni `rabadon wrong` gerçekten koşuldu ve deftere yazdı.
  **S13/e boşluğu ilan edildi:** `WRONG_REFUSAL` satırında `sess`/`sid`/`call`
  YOK, `STOP`'ta VAR — pay ile payda yalnız **kural adı** üzerinden birleşiyor,
  tek tek retle birleşmiyor. Onarımı kapsam dışı.
- **S14** — `native/gate.cpp` `kSilencers[SIL_COUNT]` tek tablo; her `Muter` o
  tablodan kuruluyor; `rabadon-gate --silencers` tabloyu ilan ediyor.
  `docs_truth_test.sh` bölüm 2b, `SITUATIONS` kümesinin ikilinin ilan ettiği
  kümeye **EŞİT** olmasını şart koşuyor. **Boş yeşil:** kaynağa geçici yedinci
  susturucu eklendi → süit **41 ok / 1 fail, EXIT=1** ve satırı adıyla söyledi;
  geri alındı → **42 ok / 0 fail** (`f2-4-bosyesil.out`). Mevcut 40 iddianın
  hiçbiri silinmedi (40 → 42). `cli_test.sh` `PRODUCT` listesine dokunulmadı,
  yeni ürün verb'ü YOK.
- **S15** — kart 5. `linux/amd64` (emülasyon, `--network none`) ve `--user
  1000:1000` non-root, ikisi de `c8a2ad6` HEAD'inde tam koştu:
  **105 süit / 102 yeşil / 3 kırmızı / 0 timeout / 0 hiç koşmayan**, her ikisi de.
  Ham çıktı `reports/refenv/` altına **commit'lendi** (S15/d kapandı).
  **NEGATİF BULGU, olduğu gibi:** F1e'nin "`publish_redaction_test.sh` kırmızısı
  root ortam artefaktıdır" iddiası **YANLIŞLANDI** — non-root'ta da kırmızı, ve
  daha kötü: uid 1000 = `node` olduğu için tek düzyazı eşleşmesi **13 dosyada
  107 eşleşmeye** çıktı. Sızıntı yok; kusur kontrolün tasarımında.

---

## 3. ADIM KAPISI (§8.6) — §5'in ADIM 3'ü GERÇEK OLDU

| soru | ölçülen cevap | kanıt |
|---|---|---|
| kullanıcı kaç adımda o ekrana varıyor | `rabadon --help` → `--signals` geçen **1** satır (derinlik 1, doküman kazısı yok) → `rabadon usage --signals`. **TOPLAM 2 komut** (bilen kullanıcı için 1), 0 ayar, 0 dosya, `real 0,02 sn` | `f2-2-adim.out` |
| ekranda ne yazıyor | 37 satır, tek ekran, ekran görüntüsü alınabilir; korpus + LOSS + beş dedektör + özet + tek ileri komut | `f2-2-ekran-snapshot.out` (verbatim, dondurulmuş yedek) |
| nasıl kaçıyor | ekran kendiliğinden `exit 0`; pager yok, prompt yok. `RABADON_DIR`'ın dosya+mtime kümesi ekrandan önce ve sonra **aynı hash** — salt okunur. Dedektörler `RABADON_SIGNALS=0` ile kapatılabiliyor | `f2-2-readonly.out`, `signals.h:63` |

**Yeni ürün verb'ü YOK.** Yüzey `rabadon usage` **bayrağı**. `cli_test.sh`
beş-verb tavanı yeşil (315/0). `rabadon scan` yazılmadı.

---

## 4. S9 — `2b` VE HOT-PATH (NEGATİF, OLDUĞU GİBİ)

**F2 hot-path'e hiçbir şey eklemedi.** `--signals` spool açılmadan önce dönen
erken-çıkışlı bir kol; `grep -c 'rabadon-stats' native/gate.cpp` → **0**.
`--silencers` `argc == 1` olan sıcak yola hiç girmiyor.

Kart 0'ın iki ikiliyi aynı dakikada ölçen bench'i: **228,7 µs → 231,8 µs
(+%1,4)** (`f2-0-bench.out`). Bu bir regresyon iddiası değil, bir bant ölçümü.

**§8.5 satırı, verbatim (DURUM.md'den, F1e ölçümü, N=300):**

| alet | sayı |
|---|---|
| süreç-içi prob (`reports/R7/accept.sh`, regresyon cetveli) | **1229,0 µs** |
| **GERÇEK `native/rabadon-gate`, uçtan uca, ham** | **3381,3 µs** (p10 3147,7 / p90 3919,0) |
| aynı harness'ta boş taban (`/usr/bin/true`) | 1386,8 µs |
| → **rabadon'a ATFEDİLEBİLİR maliyet** | **1994,5 µs** |
| tavan (`2b`) | 1000,0 µs — **hiç oynatılmadı** |
| **atfedilebilir / tavan** | **1,99×** |

**F2 kapanışında `2b` bu makinede: 1164,0 µs** (süreç-içi prob, tavan 1000 µs;
aynı ajanın faz ortasındaki okuması 1305,2 µs'ydi, ikisi de yazılıdır).
On iki ölçümün serisi: 1299,4 → 1244,2 → 1261,0 → 1310,8 → 1248,8 → 1229,9 →
1184,7 → 1270,3 → 1293,2 → 1229,0 → 1305,2 → **1164,0** µs. Sürüklenme yok,
bant içinde, **ve on ikisinin de tavanın üstünde.** Onarım **F3-S1**, bu fazın
işi değil.

**Yasak tutuldu:** sevk edilen hiçbir yüzeyde `sub-ms` / `sub-millisecond` /
"under a millisecond" **yok** (`grep -rniE` README.md, docs, site, native, bin,
npm, index.html → yalnız iki isabet: `native/export.cpp:368` bir kod yorumu ve
`native/signals_screen_test.sh:183` kelimeyi **yasaklayan** satır).
README/quickstart'ın yayımladığı sayı **~3,1 ms**'dir, prob sayısı değildir —
yani tek başına anılan bir prob rakamı sevk edilen yüzeyde YOK.

---

## 5. S10 — F2'NİN KENDİ SÜİTLERİ TEMİZ KONTEYNERDE

Kart 5'in iki koşumu (`amd64` ve non-root) **`c8a2ad6`** HEAD'indeydi ve o
noktada `native/signals_screen_test.sh` **henüz yoktu** (`943f73c` sonra geldi).
Yani S10, kart 5'in ölçümüyle **kapanmıyordu**. Bu ajan boşluğu kapattı: aynı
harness, fazın **NİHAİ** HEAD'inde.

    bash native/refenv/run.sh --out reports/refenv --prefix f2-6-arm64-root- \
         --suite-timeout 1200

LABEL: `node:22-bookworm · linux/arm64 · root · --network none`.
HEAD: `6a6b03a`.

| ölçü | F1e (105 süit) | kart 5 amd64 | kart 5 non-root | **F2 NİHAİ (bu koşum)** |
|---|---|---|---|---|
| HEAD | `6385da1` | `c8a2ad6` | `c8a2ad6` | **`6a6b03a`** |
| `make all` | exit 0 | exit 0 | exit 0 | **exit 0** |
| süit sayısı | 105 | 105 | 105 | **106** (+1 = `signals_screen_test.sh`) |
| GREEN | 102 | 102 | 102 | **103** |
| RED | 3 | 3 | 3 | **3** |
| TIMEOUT / hiç koşmayan | 0 / 0 | 0 / 0 | 0 / 0 | **0 / 0** |
| süit duvar süresi toplamı | — | 1411 s | 787 s | **731 s** |

**F2'nin kendi/dokunduğu süitleri, konteynerde, `--network none`, hepsi YEŞİL:**

    signals_screen_test.sh   0   38 ok   0 fail   GREEN     <- S10'un asıl boşluğu
    moves_test.sh            0   22 ok   0 fail   GREEN
    docs_truth_test.sh       0   42 ok   0 fail   GREEN
    heredoc_prose_test.sh    0    -      -        GREEN
    install_docs_test.sh     0   38 ok   0 fail   GREEN
    version_test.sh          0   13 ok   0 fail   GREEN
    status_truth_test.sh     0  162 ok   0 fail   GREEN
    cli_test.sh              0  315 ok   0 fail   GREEN

**Üç kırmızı, devralınan üçün AYNISI, adıyla:** `sandbox_test.sh` (8/1 — açık
CHALLENGE, O4), `site_claims_test.sh` (`gh` yok + ağ yok),
`publish_redaction_test.sh` (27/1 — `root` düzyazı eşleşmesi, sızıntı yok).
**F2 yeni kırmızı ÜRETMEDİ** (S10'un ikinci yarısı).

Ham kanıt commit'li: `reports/refenv/f2-6-arm64-root-{env,build,suites}.out`,
`f2-6-arm64-root-suites.tsv`; sürücü çıktısı
`reports/kosu/RAPOR/f2-6-refenv.out`.

**Etiket yasası (S15/e):** bu **103 yeşil**, `node:22-bookworm · linux/arm64 ·
root · --network none` hakkındadır ve **başka hiçbir şey hakkında değildir.**
"linux'ta yeşil" ya da "temiz makinede yeşil" diye genellenemez.

---

## 6. S11 / F1e-C KAPI ŞARTI

Kâtibin commit'i (`6a6b03a`) fazın SON commit'i **değildir** — bu tutanak ve
`DURUM.md` ondan sonra iner. Üçlü, fazın **NİHAİ ikilisine** karşı bu ajanın
elinde yeniden koşuldu:

| süit | sonuç |
|---|---|
| `bash native/docs_truth_test.sh` | **EXIT=0**, `docs truth: 42 ok / 0 fail` |
| `bash native/install_docs_test.sh` | **EXIT=0**, `38 ok / 0 fail` |
| `bash native/version_test.sh` | **EXIT=0**, `version: 13 passed, 0 failed` |

**Bağlayıcı şart 6 (`docs/claims.tsv` sicili):** F2'nin `docs/commands.md`'ye
yazdığı düzyazı, dosyanın `<!-- rabadon:claims-begin -->` işaretli davranış
bloğunun **DIŞINDADIR** (blok 96–125. satırlar, susturucu tablosu; yeni bölüm
185+). Bloğa tek satır girmedi, `docs/claims.tsv` bu yüzden değişmedi ve
`docs_truth_test.sh` iki yönlü sicil kontrolünü 42/0 ile geçti.
Sicilsiz iddia kalmasın diye yeni düzyazı **ayrı** bir yürütülebilir kontrole
bağlandı: `signals_screen_test.sh`'in üç yeni iddiası, sayfanın taşıdığı üç
dizgeyi (`--signals`, `LOSS`, `NOT MEASURED`) **aynı süitin gerçek ikiliden
ölçtüğü ekrana** bağlıyor. Kırmızı düşebilirliği ölçüldü:
`f2-6-kirmizi-once.out` (**35 passed, 3 failed, EXIT=1**) →
`f2-6-yesil-sonra.out` (**38 passed, 0 failed, EXIT=0**).

---

## 7. ÖLÇÜM ÜÇLÜSÜ — FAZ KAPANIŞI (bu ajan koşturdu, nihai ikili)

    make test ; echo "EXIT=$?"
    grep -cE '^[[:space:]]*ok\b' <çıktı>
    grep -oE 'PASS \([0-9]+ checks?\)' <çıktı> | grep -oE '[0-9]+' | paste -sd+ - | bc
    npm test
    bash reports/R7/accept.sh

Üçü de **kâtip commit'inden (`6a6b03a`) SONRA**, nihai ikiliye karşı koşuldu.

| ölçü | F1e sonrası (taban) | **F2 sonrası** |
|---|---|---|
| `make test` exit | 0 | **0** |
| native iddia (GENİŞ `^[[:space:]]*ok\b`) | 3738 | **3786** (+48) |
| native `PASS (N checks)` toplamı | 626 | **633** (+7) |
| native toplam | 4364 | **4419** |
| `npm test` | 64 pass / 0 fail | **64 pass / 0 fail**, exit 0 |
| **TOPLAM** | 4428 | **4483 yeşil / 0 kırmızı** (+55) |
| `bash reports/R7/accept.sh` | 23 yeşil / 3 kırmızı | **23 yeşil / 3 kırmızı**, exit 1 |

Ham çıktı, commit'li: `reports/kosu/RAPOR/f2-6-maketest.out`,
`f2-6-npmtest.out`, `f2-6-r7accept.out`, `f2-6-f1ec-uclu.out`.

Silinen / zayıflatılan / atlanan / yeniden adlandırılan test **YOK**.
Eşik / tolerans / fikstür / ön-kayıt **HİÇ** değişmedi.
Tek istisna ve tek kabul-dosyası değişikliği §8'de adıyla yazılı (`8a`).

`make test` çıktısındaki tek `FAIL` dizesi `regression_demo.sh`'in
FİKSTÜRÜDÜR; o süit `regression: 4 passed, 0 failed` diyor, exit 0.

---

## 8. KIRMIZI AD KÜMESİ (§8.3)

**F2 SONRASI: `{ 2b, 6e, 7b }` — BÜYÜMEDİ.**
`bash reports/R7/accept.sh` → exit 1, 23 yeşil / 3 kırmızı.
- `2b` — gate medyanı **1164,0 µs**, tavan 1000 µs. F0'dan beri kırmızı, F3-S1.
- `6e` — arm B'de `estimated_saved` toplamı ya da arm başına `total_cost_usd` yok.
- `7b` — sapma hesaplanamadığı için falsifikasyon 2 UNCHECKABLE.

**Faz İÇİNDE geçici olarak dörde çıktı ve kapandı:** kart 1'in yeni CLAIM 8'i
`moves_test.sh`'i 21 → 22 iddiaya taşıdı, `reports/R7/accept.sh` `8a` ise sayıyı
**tam eşitlikle** pinliyordu; ad kümesi `{2b, 6e, 7b, 8a}` oldu. Kart 1 işçisi
**onarmayı reddetti** (kabul dosyası, CLAUDE.md 2) ve CHALLENGE yazdı
(`f2-1-kart.md` sonu). **Hüküm:** kabul kriterini uygulayan değil, ayrı bir
işçi (kart 3) kendi commit'inde (`9cba3cd`) 21 → 22 yaptı ve **tam eşitliği
korudu** (`>=` yapmadı). `8a` bugün yeşil.
**BU BİR KABUL DOSYASI DEĞİŞİKLİĞİDİR VE HAKEMİN ONAYINI BEKLER** — bağlayıcı
şart 3 "kabul betiği dokunulmaz" der; dokunan uygulayan olmadı ama karar yine de
insanın. Sessiz geçmiyorum.

Test süitlerinde kırmızı ad **YOK**. Konteynerdeki üç devralınan kırmızı
(`sandbox_test.sh` CHALLENGE/O4, `site_claims_test.sh` `gh` yok,
`publish_redaction_test.sh` düzyazı eşleşmesi) **F2'nin açtığı değildir** ve F2
**yenisini üretmedi**.

---

## 9. NOT VERIFIED (boş değildir)

1. **`2b`'yi bu ajan uçtan uca yeniden ölçmedi.** §4'teki 3381,3 / 1994,5 µs
   satırı F1e'nin `f1e-4-2b-iki-sayi.out` ölçümüdür; bu turda yalnız süreç-içi
   prob (1305,2 µs) koşuldu.
2. **`linux/amd64 · non-root` köşesi hâlâ boş.** Kart 5 iki ekseni tek tek
   oynattı; kesişim ölçülmedi.
3. **Hâlâ yalnız `node:22-bookworm`.** CLAUDE.md'nin "yalnız git ve shell"
   barı ölçülmedi; `debian:bookworm-slim` ve `rabadon-refenv:git-and-shell`
   koşulmadı. "102 yeşil" o makine hakkında hiçbir şey söylemez.
4. **Eksen başına tek örnek.** Flaky bir süit ile kararlı olan bu ölçümde
   ayırt edilemez.
5. **Sinyal isabet oranı bilinmiyor.** `scope_drift` 17/17 yanlış çıktı ama
   diğer dördü `n=0`; ekran "0 etiketli" diyor ve **etiketi girecek bir komut
   yok** — etiketleme yüzeyi ürüne girmedi (kapsam dışıydı).
6. **`--signals` yalnız bu makinenin ve konteynerin korpusuyla ölçüldü.**
   Başka bir kullanıcının ring'i, bozuk/yarım bir ring, farklı `CAP` — hiçbiri
   denenmedi.
7. **16 retin 4'ü HÜKÜMSÜZ kaldı** (`STOP.detail` 160 bayt kesme). Yanlış
   pozitif oranının üst sınırı budur ve daha iyi bilinmiyor.
8. **`WRONG_REFUSAL` ↔ `STOP` birleştirmesi kural adı üzerinden yapıldı**,
   tek tek retle değil; 10–13'ün "operatör zaten `wrong` koştu" hükmü `ts`
   yakınlığına dayanıyor, birebir bağ KURULAMADI.
9. **`bin/rabadon.mjs` okunmadı** (donuk anti-path, O3). S5'in JSON kaçışlama
   kusurunun aynısının JS tarafında olup olmadığı **DOĞRULANMADI** — ve JS
   gate aynı deftere yazan ikinci bir yazardır.
10. **`make bench`, `make precision`, `promises_test.sh`, `disclosure:` hedefi**
    konteynerde koşulmadı.
11. **Ölçüm üçlüsü kâtip commit'inden sonra macOS'ta BİR KEZ koşuldu.**
    `2b` bu makinede yük bağımlıdır (1164–1305 µs bandı aynı gün); zamana
    duyarlı başka kontroller de olabilir, tekrar edilmedi.
12. **Konteyner koşumu `6a6b03a` HEAD'indedir; bu tutanak ve `DURUM.md`
    ondan sonraki commit'lerdir.** İkisi de yalnız `reports/` altında
    düzyazıdır, tek satır kod ya da test değiştirmezler — ama bu bir
    argümandır, o commit'lerde konteyner yeniden koşulmadı.

---

## 10. PARKED (kapsam dışı, diff'e girmedi)

1. **Tek bir oturum tüm kaybı üretiyor.** `286fd71d…` canlıda 1.617 hamle
   saydı, 200 tuttu — `CAP=200` uzun bir oturumun kaydının **%88'ini** siliyor.
   R1'in "sekansı yazmak" vaadi tek uzun oturumda fiilen çalışmıyor.
2. **`STOP.detail` 160 baytta kesiyor.** Bir retin neden verildiği sonradan
   denetlenemiyor; yanlış-pozitif sayımının tavanı bu.
3. **`publish_redaction_test.sh` yapısal olarak geçemez** root ya da uid 1000
   altında (`root`/`node` ikisi de düzyazıda geçen kelimeler). Muafiyet listesi
   tek bir sabit ad (`runner`) taşıyor.
4. **`reports/R7/accept.sh` `8b`** (`signals_test.sh 39/0`) aynı tam-eşitlik
   tuzağını taşıyor; `signals_test.sh`'e ilk eklenen iddiada aynı şekilde
   kırmızı düşer.
5. **`no-gnu-timeout-on-macos` kuralının gerekçesi bu makinede yanlış**
   (`/opt/homebrew/bin/timeout` var). `guard.json` yasak olduğu için yalnız
   `rabadon wrong` ile deftere yazıldı.
6. **`~/.rabadon/wrong-<kural>` yetim izin dosyaları** defterden bağımsız
   ikinci bir gerçek kaynağı (`wrong-red-base`, `wrong-no-exit-code-after-pipe`).
7. **`native/gate.cpp` kHelp `--silencers`'ı listelemiyor** (`--wrong` da öyle).
8. **RepoDigest iki farklı mimari imajı için AYNI dizgeyi veriyor.** Imaj
   kimliğiyle pinle, bu store'un RepoDigest'iyle asla.
9. `rabadon dev stats --signals` aynı ekranı basıyor (aynı verb, ikinci yüzey
   değil) — belgelenmedi.
10. **Paylaşılan çalışma ağacı canlıydı.** Kart 1 başka bir işçinin
    commit'lenmemiş `gate.cpp`'sini derleyip ölçtü; `reports/refenv/` bir kez
    koşum ortasında silindi. Paralel işçi + tek ağaç = ölçüm kirliliği; koşu
    yapısı sorunu, faz sorunu değil.

---

## 11. CHALLENGE

**Açık CHALLENGE (F2'nin açtığı değil, devraldığı):** `native/sandbox_test.sh:121`
konteynerde KIRMIZI. Ürün `NO **usable** kernel backend` basıyor, test
`"no kernel backend"` arıyor; dizgeyi `a74e7d8` (31 Tem) değiştirdi ve testi
güncellemedi. **Kırmızı bırakıldı, insan hükmü bekliyor** (O4, CLAUDE.md 1).
F2 dokunmadı.

**Kapanan CHALLENGE:** kart 1'in `reports/R7/accept.sh` `8a` tam-eşitlik
itirazı — §8'de yazılı, hakemin onayını bekleyen bir kabul-dosyası
değişikliğiyle çözüldü.

**S12/d CHALLENGE'ı AÇILMADI**, çünkü gerekmedi: `bash -c "<gerçek boru hattı>"`
hücresi ölçüldü ve doğru cevabı (BLOCK) verdi.

---

## 12. KENDİ ÜRÜNÜMÜZ BU FAZDA BİZİ KESTİ (doğru red)

Bu ajanın `grep … | head -20; echo "EXIT=$?"` komutu `no-exit-code-after-pipe`
ile reddedildi ve **red doğruydu** — `$?` `head`'in durumuydu. Komut düzeltildi,
kural gevşetilmedi. Kart 0'ın onardığı yanlış-pozitif ailesi (tırnaklı düzyazı)
bu turda bir kez bile ateşlemedi; bu tutanak `git commit -m` ve heredoc ile
yazıldı ve **kesilmedi** — S12'nin onarımının canlı kanıtı.
