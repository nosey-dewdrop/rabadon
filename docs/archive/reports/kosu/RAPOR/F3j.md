# F3j — kart

**KART SAYISI: 4. TAHMİNLER (§3.12, iş başlamadan yazıldı ve commit'lendi `a2c251f`, 2026-08-30 05:06):**
K1 self-heal yol yasası + süit **90 dk** · K2 B3 üçlü kapısı + stub yakalama **60 dk** ·
K3 `blind spots:` eşitlik kilidi **75 dk** · K4 `2b` `wait` bacağı **60 dk**. **Toplam 285 dk; iki katı 570 dk — aşılmadı.**

**K1 — B2 KAPANDI, KÖK SEBEBİ ÜRETEREK.** `installHooks` **koşan** ikilinin mutlak yolunu yazıyordu. Yasa değişti: kayıtlı komutun ikilisi VARSA komut **bayt bayt taşınır** (yükseltme olayla ilgilidir, adresle değil); yol yazmak zorunda kalınan tek dalda **kanonik** yol yazılır ve **dayanıklı** olduğu ölçülür (`.git` bir DOSYA ise worktree, `os.tmpdir()` altı geçici → **yazmaz, sesli reddeder**, `rabadon init` der); her repoint **eski+yeni adresle** ilan edilir, `added.length`'ten bağımsız; yazdığını doğrular. `native/selfheal_path_test.sh` **21/0**; onarımsız ikilide **10/21 KIRMIZI** (`f3j-k1-bosyesil.out`), eski davranışı geri koyan tek satırlık mutantta yine **10 kırmızı**, geri alınca 21/0.
**YAN BULGU (ürün kusuru, kart açılmadı):** `refresh.mjs` gövdesi `import.meta.url == file://$argv[1]` şartına bağlı; macOS'ta `/tmp` sembolik olduğu için bu şart **yanlış** olur ve self-heal **hiç koşmaz**. Fikstürüm önce bu yüzden defekti "yok" göstermişti.

**K2 — B3 ÜÇLÜ KAPISI KURULDU.** `native/machine_intact_test.sh`, `make test`'in **ilk ve son** satırı (`record` / `verify`). Üç özne: 20 sevk edilen ikilinin **mod+boyut+sha256**, `~/.claude/settings.json` **bayt bayt**, canlı fren. **Stub'ı yakalar:** boyut tabanı 1024 bayt + derlenen her ikilide gerçek Mach-O/ELF sihirli sayısı + `--version` boş olamaz. **Beş mutasyon kırmızı düşürdü:** 17 baytlık `exit 0` puçu (4/2) · `chmod 644` (5/1) · settings.json değişmesi (8/1) · frenin oynaması (8/1) · **`record` kolunun silinmesi (5/1)** — kapı boşa düşemez.

**K3 — EKRAN 7 SAYIYORDU, ÖLÇÜM 21.** Süidin üç kolu da **tek yön** soruyordu ("ilan edilen gerçekten kör mü"); ilan edilmeyen kör şekli hiçbiri göremezdi. `native/lawblind_corpus.txt` (36 aday) sevk edilen ikiliyle okundu: **21 ALLOW+GONE · 10 REFUSE · 5 ALLOW+THERE** (`f3j-k3-korpus-olcum.out`). Kilit artık **EŞİTLİK**, iki yönlü: korpusta ALLOW+GONE ölçülen her şekil ilan edilmiş olmalı, ilan edilen her şekil korpusta olmalı. `kLawBlind` 7→**21**, sınıf 2→**4** — yeni iki sınıf: **`cd .. && mv proj proj.gone`** (hiçbir şey silinmiyor, yasa sadece artık orada değil) ve **`rm -rf "$PWD"`**. **Şekiller KAPATILMADI**, gerekçe yerinde. `law_blind` **10/0 → 15/0**, düşen iddia yok; üç mutasyonda kırmızı (satır düş → 12/3, korpus dışı satır ilan et → 12/3). `docs/guard.md` eski cümleyi **silmeden** ölçüm tarihi+komutuyla düzeltti.

**K4 — BU FAZ DA HIZLANDIRMADI.** Zorunlu aletle (`2b-uctan-uca.sh`, N=200) üç kol dönüşümlü, 8 tekrar (`f3j-k4-uc-kol.out`): canlı daemon **8/8** yavaş — hakemin işareti **yeniden üretildi**. **AMA `wait` bacağı `2b`'nin yolunda DEĞİL:** sevk edilen hiçbir şey `rabadon-gated`'i başlatmıyor (`grep` → yalnız `rabadon dev gated`), yani kusursuz bir `wait` onarımı `2b`'yi **sıfır** µs oynatır. Tavan oynatılmadı, `accept.sh`'e dokunulmadı, "yaklaştı" yazılmadı. **YENİ BULGU:** makinede **`/tmp/rabadon-501.sock` 24 Ağu'dan beri dinleyicisiz duruyor**, yani her gate çağrısı ölü bir `connect()` ödüyor; 12 eşli tekrar **9 artı / 3 eksi**, tek yanlı DEĞİL (yük ortalaması 9) → **boyutunu ÖLÇEMEDİM**. Sahibinin dosyası, **silmedim**.

**KAPI.** `make test` **EXIT=0** · native **4086** + `PASS (N checks)` **633** + `npm test` **64/0** = **4783 yeşil / 0 kırmızı** (taban 4742, **+41**). +41'in tamamı süit-diff'iyle ayrıldı: `selfheal_path` yok→**21**, `machine_intact` yok→**15**, `law_blind` 10→**15**. **Küçülen 0 · kaybolan 0 · silinen/zayıflatılan/atlanan test 0.** Süit **119 → 121**. `bash reports/R7/accept.sh` **EXIT=1, 23 yeşil / 3 kırmızı**, ad kümesi **`{2b, 6e, 7b}` BÜYÜMEDİ**. **F1e-C üçlüsü nihai ikiliye karşı: 42/0 · 38/0 · 13/0**; kâtip commit'i (`6196f8f`) fazın son commit'i değil.

**FAZ SONU ÜÇLÜSÜ, ÖLÇÜLDÜ.** `~/.claude/settings.json` faz boyunca **`adcb41a93f858d6b`**, bayt bayt aynı (K1 mutasyonları ve `make test` dahil, her turda yeniden ölçüldü). 20 ikilinin **20'si de `-rwxr-xr-x`**; `F3j-oncesi`'ne göre yalnız **ikisi** değişti (`rabadon-gate`, `rabadon-gated`) ve sebebi commit'li `gate.cpp` `kLawBlind` düzeltmesidir — **18'i bayt bayt aynı**. Fren: `mode=watch`, `enabled` **YOK**, mtime **30 Ağu 03:22** — sahibinin bıraktığı hâl, **dokunulmadı**. Worktree **açılmadı** (fikstür `.git`'i dosya olan bir kopyayla taklit edildi, gerçek worktree yok).

**§4.3 — YANLIŞ POZİTİF ADAYI: 0.** Bugünkü defterdeki `no-delete-native-binaries` ve `baseline-law-unmade` ateşlemeleri **doğru retlerdir**: birincisi K2'nin kasıtlı stub mutasyonuna, ikincisi K3 korpusunun fikstür `guard.json`'larını kasten yok etmesine bastı. Kural yanlış ateşlemedi.

**ÖLÇEMEDİKLERİM.** (1) stale socket'in µs maliyeti (yük 9, tek yanlı değil). (2) B1'in faili — K2 kapısı olayı yakalar, faili değil. (3) Konteyner/`refenv` bu fazda koşulmadı; `machine_intact` ve `selfheal_path` yalnız macOS'ta ölçüldü. (4) `2b` daemonlu kolun kesin µs'si (yük altında mutlak sayı anlamsız; yalnız **işaret** 8/8 geçerli).
