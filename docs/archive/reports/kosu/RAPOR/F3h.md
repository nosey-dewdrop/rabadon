# F3h — 4 kart. Tahminler (§3.12, koddan ÖNCE, commit `70884bc`): K0 90dk · K1 120dk · K2 120dk · K3 90dk = 7 sa; iki katı 14 sa, **aşılmadı**.

**K0 · settings.json — GEÇTİ, suçlu hakemin sandığı beş süit DEĞİL.** Canlı dosyayı `make test` etrafında hash'leyerek ölçtüm: yazan **tam iki** süit — `contract_test.sh`, `promises_test.sh`. Hakemin şüphelendiği beşi (`doctor`/`exit_path`/`failed_call`/`hook_upgrade`/`npm_install`) **temiz** ölçüldü, kendi HOME'unu zaten kuruyorlar. Sebep kaynaktan: her kol taze `RABADON_DIR` → self-heal damgası yok → `refresh.mjs` → `os.homedir()`. İkisi artık kendi HOME'unu ilan ediyor; `RABADON_SELFHEAL` **kapatılmadı**, yalnız adres yazıldı. Kilit `native/home_isolation_test.sh` (6 iddia): decoy home + **kontrol kolu** (izole EDİLMEMİŞ SessionStart hâlâ yazmalı), yoksa kilit boşa düşer. Kanıt `kanit/f3h/k0-settings-leak.md`.

**K1 · BEŞ YANLIŞ POZİTİFİN BEŞİ DE KAPANDI.** `mkdir` yalnız YARATIR (mod bayrağı hariç) · `tar` MODA göre yargılanır (`-c` arşivi yazar, yolları OKUR) · `-not`/`!` altındaki find yüklemi yasayı DIŞARIDA BIRAKIR · yasanın öznesi artık gerçekten **bu projenin** kopyası. Kural **gevşetilmedi, kapsamı daraldı**: her daraltmanın ikizi aynı süitte kırmızı düşebiliyor (`mkdir -m 000`, `tar -xf … -C .rabadon`, `find … -path … -delete`, mutlak yazılmış kendi kopyamız). `docs/guard.md` **silinmeden** düzeltildi: ölçüm tarihi, probe komutu, beş satırlık REFUSE çıktısı, daralan özne.

**K2 · ALTI ŞEKİL DAHA, ÜÇÜ KİMSENİN LİSTESİNDE YOKTU.** Glob (`.r*`, `.rabado?`, `.*`, `.[r]abadon`) fnmatch+FNM_PERIOD ile; `rm -rf *` hâlâ geçer, çünkü bash da onu nokta'ya açmaz. Satır-içi yorumlayıcı gövdesi (`python3 -c`/`perl -e`/`node -e`/`ruby -e`) kapandı, **dosyayla** verilen program dokunulmadı. Listede olmayan üç bulgum: **`awk 'BEGIN{system(...)}'`**, **`mv .rabadon{,.bak}`**, ve **`ls | xargs rm -rf`** — sonuncusu KAPANMADI ve kapanamaz (yol komut metninde hiç geçmiyor), yazıldı. **AÇIK, ad düzeltilerek:** `tar -xf … -C .` · `mv . ../renamed` · `cd .. && rm -rf proj` · `git worktree remove --force` — dördü de ağacın tamamını hedefler; kesmek kullanıcıyı kendi projesine hapsederdi.

**K2 · ÇIKIŞ KAPISI ARTIK İDDİA.** Kendi eski projeni silmek geçiyor (`rm -rf ./old-project`, `mv ./old-project /tmp/bin`) ve `disabled["baseline-law-unmade"]` gerçekten açıyor — üçü de testte. Dışarıdaki bir `.rabadon` artık **`baseline-rm-rf-outside`** adıyla reddediliyor, yani ekranın verdiği tek komut doğru kuralı gösteriyor. `RABADON_OFF=1` yanlış adresti; belgede öyle yazıldı.

**K3 · 911 µs NEREYE GİDİYOR — ÖLÇÜLDÜ, BACAK SUÇLANMADI.** (a) **433,1 µs'si `2b`'nin kendi kurduğu daemon gidiş-dönüşü** — sekiz eşli turda **8/8 tek yanlı** (+412,8…+476,6); soket kapatılınca gidiyor, ve `2b` soketi export eden tek koldur. (b) **265 µs'si defterin kendi büyümesi** — taze `$HOME`, 3/3: ~160 çağrıdan sonra 792 µs, ~1660 çağrıdan sonra 1066 µs. (c) Kalanı sürüklenme: tek koşu içinde aynı kol 841→1172 µs (**+%39, kod değişmeden**), yani 1942,8-vs-1031,5 zaten iki ayrı oturumdu. **BU FAZ DA HIZLANDIRMADI; "yaklaştı" yazmıyorum** — 433,1 µs bandı (|439|) aşmıyor, iddia değil KONUM. Tavan 1000 µs oynatılmadı, `accept.sh` diff'te yok. Kendi kodumun bedeli: HEAD − F3h-oncesi **+53,0 µs medyan, 6'da 5 pozitif, tek yanlı DEĞİL, bant içinde** — sıfır demiyorum. Ayrıntı `kanit/f3h/k3-911.md`.

**KAPI.** `make test` **EXIT=0** · **4719 yeşil / 0 kırmızı** (native 4022 iddia + 633 kontrol + npm 64/0; taban 4664, **+55**) · süit 116→117, **küçülen 0, kaybolan 0**; değişen yalnız `law_family 68→117` ve yeni `home_isolation 0→6`. `accept.sh` EXIT=1, 23/3, **`{2b, 6e, 7b}` BÜYÜMEDİ** (`2b` bugün 1300,6 µs). Mühürlü set (`accept.sh` · `ON-KAYIT.md` · `claims.tsv` · `guard.json` · korpus) ve `bin/rabadon.mjs` (O3) diff'te **HİÇ YOK**; `CAP=200` yerinde; silinen/`skip` iddia **0**. F1e-C üçlüsü nihai ikiliye karşı **42/0 · 38/0 · 13/0**, kâtip commit'i (`2d4b7b4`) fazın sonuncusu **değil**. Ölçüt commit'leri `4d9724a`/`c2066bb`/`b78ec6b`, kodları `febf4b9`/`56e0489`/`c12b08b` — ayrı ve önce.

**MUTASYON + BOŞ YEŞİL.** Boş yeşil turu `F3h-oncesi` artefaktında: yeni kollar **97 yeşil / 20 KIRMIZI**. M1 (FNM_PERIOD düşürüldü) → 2 kırmızı · M2 (`mkdir` koşulsuz muaf) → 1 kırmızı · M3 (kapsam daraltması kaldırıldı) → 1 kırmızı · K0 mutantı (izolasyon satırı silindi) → 4/2 kırmızı. Dördü de geri alındı, süitler yeşile döndü.

**KAPI BENİ KESTİ (§ adıyla).** `sed -i` ile kendi test dosyamı mutasyona uğratmak `no-blind-inplace-source-rewrite` ile kesildi; **blokajı aşmadım** — `gate.cpp:4703-4716` bileşik-komut deliği kullanılmadı, `guard.json`'a dokunulmadı, `rabadon off` çağrılmadı, yaklaşımı değiştirip mutasyonları görünür Edit diff'i olarak yaptım. Ayrıca `mkdir -p …/.rabadon` beni **canlı kesti** — K1'in kapattığı sınıfın ta kendisi.

**`~/.claude/settings.json` faz sonunda faz öncesiyle BAYT BAYT AYNI** (`6c14cc5f…`, `cmp`). Worktree `$HOME/.rb-f3h-wt` **kaldırıldı**; F3g'nin diskte bıraktığı `_f3g_kum`, `_hakem_f3g_lab`, `settings.json.hakem-f3g-save` de temizlendi.

**ÖLÇEMEDİKLERİM.** Konteynerde hiçbir şey koşmadı, yalnız konak macOS · leg 3'ün içi (`perf`/`dtrace` sudo istiyor) · daemon yolunun NEDEN pahalı olduğu (nerede olduğunu ölçtüm, nedenini değil) · K3-bulgu-2'nin yazma mı okuma mı kilit mi olduğu · `ls | xargs rm -rf` sınıfı · **(c) negatif kontrolü kapsam dışıydı, koşulmadı → F4 HÂLÂ KAPALI.**
