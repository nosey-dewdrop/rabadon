# F3c — kart (2026-08-29)

## TAHMİN (§3.12 — kart kesilmeden ÖNCE yazıldı ve commit'lendi)

Kart sayısı: **4**. İki katı = **8**. Sekizi aşarsam durup hakeme giderim.

| kart | ne | tahmin |
|---|---|---|
| 1 (BLOKLAYAN) | kanıt kalıcılığı: `INJECT`'e `psig` + yeni `INJECT_ANSWER` + enjeksiyonu uçtan uca süren ilk fikstür süiti, mutasyon kanıtı + dolu halka | **~2,5 sa** — en büyüğü, ürün kodu + yeni süit + iki mutasyon |
| 2 | KOSU §F3 ürün kapsamı: (a)/(b)/(c) üç katmanlı kabul, canlı hook, merdiven | **~1,5 sa** — ŞÜPHELİ: (c) negatif kontrolü ağ + `claude -p` + saatler ister, muhtemelen ÖLÇEMEM |
| 3 | F3-S1 kalanı: `2b` uçtan uca, taban 1475,4 µs, tavan 1000,0 µs, açık 475,4 µs | **~1 sa** — ŞÜPHELİ: gürültü bandı ±%20, 475 µs'lik açık tek fazda kapanmayabilir |
| 4 | `native/make_deps_test.sh:233` sessiz atlama dalı — ölç, adıyla ilan ettir | **~20 dk** |

Tahminin kendisi bu commit'tedir; kart sonuçları aşağıya EKLENİR, bu blok değişmez.

## SONUÇ — 4 kart kesildi, tetik çalmadı (4 < 8)

**KART 1 — KAPANDI.** `INJECT` artık `psig` taşıyor (diagnoz kurulurken tekrarlayan imza, kuyruğa girerken alınır — teslimde değil), ve teslimden sonraki İLK hamle `INJECT_ANSWER` olarak yazılıyor (`psig` → `sig`, `same`). İkisi de append-only, hash-zincirli spool'da; (b) artık halkaya değil **iki defter satırına** sorulan bir soru. Süit: `native/inject_answer_test.sh`, **16 iddia**, ölçüt koddan ÖNCE ayrı commit'te (`d9a4907`, 6/1 KIRMIZI), onarım sonra (`087e559`). Halka gerçekten yuvarlandı ve kayıp yeniden üretildi: **başlık 218 hamle sayıyor, CAP 200, `mseq=6` tahliye edilmiş (en eski tutulan 18)** — ve (b) o hâlde HÂLÂ cevaplanıyor. `CAP` büyütülmedi, korpus/halka silinmedi, snapshot'a dokunulmadı. **Mutasyon kanıtı, üçü de kendi elimle (`kanit/f3c/1-mutasyon.out`):** psig'i düşür → 14/1; cevap-borcu bayrağını hiç temizleme → 1 enjeksiyona **211 cevap**, 15/1; `same`'i `false`'a sabitle → 15/1. Üçü de geri alındı, 16/0. **Boş yeşil turu:** aynı süit `F3c-oncesi` (`94fa1ec`) artefaktı üstünde **6/1 KIRMIZI** (`kanit/f3c/1-bosyesil.out`). **Bir mutasyon TUTMADI ve yazıyorum:** yalnız `injAnsAfterSeq = -1` satırını silmek süiti kırmızı düşürmedi — serileştirici `injAnsSignal` boşken alanı zaten yazmıyor, yani "enjeksiyon başına tek cevap" **iki ayrı kilitle** tutuluyor. Süit tek kilidin kaybını göremez; ikisinin birden kaybını görür.

**KART 2 — KISMİ, ve eksik kısım ölçülemez.** Sevk edilen ikiliye karşı (`kanit/f3c/2-abc-olcum.out`): **(a) KARŞILANDI** — bir oturumun defterinde `SIGNAL`×1 + `INJECT`×1 + `COUNTER`×1 (`STEP_START`×8 sayılmadı). **(b) CEVAPLANABİLİR** — `oscillation`, `psig=5d8f862e…` → `sig=ba0290a3…`, `same=false`, yargılanabilir **n=1**. **CANLI defterde n=0 ve sebebi bu:** oradaki **7 INJECT'in 0'ı psig taşıyor**, hepsi bu fazdan eski. Sayaç bu commit'ten sonra sıfırdan başlar; 0 mekanizmanın çalışmadığı değil, henüz canlı oturum geçmediği anlamına gelir. **(c) KOŞULMADI, ÖLÇEMEDİM** — iki kollu negatif kontrol ağ + `claude -p` ister, görev kümesi §3.8/2 ile mühürlü. §F3: *"Bu ölçüm yapılmadan F4 açılmaz."* → **F4 AÇILMAZ.** **Merdivenin üçüncü basamağı (blok) YAPILMADI**, ve gerekçesi ölçülmüş bir sayıdır: §F3 basamağı *"aynı imza devam ediyorsa"* diye tanımlıyor, o olguyu üreten alan (`same=true`) bu fazda doğdu ve canlı yanlış-pozitif oranı **n=0**. n=0 bir olguya dayanıp exit kodu değiştirmek, ürünün reddettiği compound error'un ta kendisi olurdu. Ürün diff'i artık `inject.h`'e komşu: `gate.cpp` + `docs/agent-contract.md`.

**KART 3 — ULAŞILMADI, sayı ve açık aşağıda (`kanit/f3c/3-2b-uctan-uca.out`).** Alet `kanit/f3/2b-uctan-uca.sh`, N=200, 8 eşli tekrar, gate exit sağlamlık kontrolü her koşuda geçti. **HEAD ortalama 1424,1 µs · tavan 1000,0 µs · KALAN AÇIK 424,1 µs (1,42×).** Tavan oynatılmadı, `accept.sh`'e dokunulmadı (mühürlü dörtlüde **0 satır** diff). F3b hakeminin 1475,4/475,4'ünü **silmiyorum ve uzlaştıramıyorum**: aynı `F3c-oncesi` ikilisini bugün iki kez ölçtüm, **1304,6 ve 1374,2** çıktı — hiçbir şey değişmeden alet ~70 µs kayıyor, tek eşli fark ±660 µs saçıyor. Yani 424,1 ile 475,4 farkı bu aletin çözünürlüğünün ALTINDA; ikisi de yazılıdır. **Bu fazın kendi kattığı maliyeti de ölçtüm ve olumsuz çıktı:** kart 1'in ilk hâli olay başına üç koşulsuz `get_str` taraması ekledi → eşli **+101,8 µs, 8'de 6 pozitif**. Okumaları elde olan bir anahtarın arkasına aldım (`a42ff95`) → **+50,0 µs, 8'de 4** = artık gürültüden ayrılmıyor. **Yanlışlanamayan bir iyileşme iddia etmiyorum: bu faz `2b`'yi ölçülebilir biçimde HIZLANDIRMADI.** Bir de yeni sayı: boş bir C++ `main` `/usr/bin/true`'ya göre yalnız **70,7 µs** — yani tavanı tutturmayı engelleyen şey süreç başlatma değil, rabadon'un kendi işi.

**KART 4 — KAPANDI.** `make_deps_test.sh`'in "ağaç derlenmemiş" dalı artık `skip` değil `bad`. Dalın **erişilebilir olduğunu ölçtüm** (yalnız `native/rabadon-gate` derlenmiş ağaçta süit **5/1 KIRMIZI**, mesaj arm'ı ve tek onarım komutunu adıyla söylüyor); `make all` sonrası **7/0**. `make test` içinde `test: all` yüzünden dala girilmiyor, sayaç değişmedi.

## KAPI — hepsi bu fazın NİHAİ ikilisine karşı, kendi elimle koşuldu
`make test` **EXIT=0** (doğrudan ölçüldü, dolaylı değil) · GENİŞ **3832** + `PASS (N checks)` **633** = native **4465** · `npm test` **64/0** → **TOPLAM 4529 yeşil / 0 kırmızı**. Taban 4513'ten **+16**, ve +16'nın tamamı `inject_answer_test.sh` — aritmetik kapanıyor (3832−3816=16), hiçbir süit düşmedi. `bash reports/R7/accept.sh` → EXIT=1, **23 yeşil / 3 kırmızı**, ad kümesi **`{2b, 6e, 7b}` BÜYÜMEDİ** (`2b` süreç-içi 1276,4 µs). F1e-C üçlüsü: **42/0 · 38/0 · 13/0**. Kâtip: `docs/agent-contract.md` bu fazda GERÇEKTEN değişti (yeni `INJECT_ANSWER` zarfı belgelendi) ve o commit fazın SON commit'i değil — şart iki fazdır düştüğü boşluktan çıktı. Silinen dosya **0**; `accept.sh`/`ON-KAYIT.md`/`claims.tsv`/`.rabadon/` diff'i **0 satır**. Worktree `--detach` ve `/tmp` dışındaydı, **kaldırıldı**. Kapı beni hiç kesmedi; `guard.json`'a dokunmadım, `rabadon off` kullanmadım, CHALLENGE-3 deliğini kullanmadım.

## ÖLÇEMEDİM / NOT VERIFIED
**(c) negatif kontrolü** — koşmadım, koşamam (mühürlü görev kümesi + ağ + `claude -p`). **Canlı (b)** — fikstürde n=1, canlı defterde **n=0**; gerçek ajan oturumunda hiç doğrulanmadı. Her şey **macOS 24.2.0, tek makine, arm64**; konteyner/x86 koşumu **YOK**, yeni süit yalnız burada koştu. `2b`'nin mutlak sayısı **±%25 bandında**, tek koşudan alıntılanmamalı — bu fazın +50 µs'lik farkı ölçülemedi, **yok olduğu kanıtlanmadı**. `INJECT_ANSWER` yalnız YENİ hamlede yazılıyor: enjeksiyondan sonraki ilk olay bir PostToolUse tamamlamasıysa cevap bir sonraki hamleye kayar (tasarım böyle), **gerçek trafikte sıklığını ölçmedim**. Kartıma ait olmayan iki artık dizini gördüm ve **dokunmadım**: `~/damla_projects_2026/_f3b_prof` ve `hakem-sbx-a` (git worktree kaydı yok).
