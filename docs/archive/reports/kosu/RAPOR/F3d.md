# F3d — kart

**§3.12 TAHMİN (koddan ÖNCE yazıldı, `c4e7fdd`, 20:51; ilk kart commit'i `af0a3b5`).**
**KART SAYISI: 3.** K1 (canlı (b)) **150 dk** · K2 (sessiz skip) **90 dk** · K3 (`2b`) **60 dk** = **300 dk**. Gerçekleşen ~**95 dk**; tetik çalmadı.

**K1 — CANLI (b): n = 0 → n = 1. Dört fazlık boşluk kapandı.** Kanıt `kanit/f3d/5-canli-b.out`. Bu ajan oturumunda (`sess 5fa2e187…`, `pipe rabadon:session`, fikstür DEĞİL), sevk edilen `native/rabadon-gate`: `SIGNAL root_migration` → `INJECT mseq=757 psig=1a37b823ede897e7 chars=400` → `INJECT_ANSWER mseq=758 psig=1a37b823ede897e7 sig=69f067c23b54e9ca **same=false**`. Enjeksiyon metni gerçekten context'e girdi (taşıyıcı çağrının `additionalContext`'i olarak).
**AMA "ajan okudu" DEMEK DEĞİL.** Enjeksiyon zaten uçmakta olan çağrıya bindi ve sonraki hamle nasılsa farklı türdendi. §F3:142 (b)'yi *sonraki hamlenin imzası* diye tanımlar, kayıt odur; nedensellik (c) olmadan kurulamaz. **(c) yine koşulmadı → F4 KAPALI.**
**(b) NEDEN 4 FAZDIR 0'DI — üç ölçüm.** (1) Enjeksiyona izinli 4 sinyalin **2'si 29 günlük defterde hiç ateşlemedi**: `oscillation` 0, `green_redefined`'ın "only the test side" kolu 3479 SIGNAL'de **0**. Canlı ateşleyen iki `green_redefined` kolu ise tam da konuşmasına izin verilmeyenler (493 + 34). (2) `err_sig` `tool_response` **blob'unun tamamı** üzerinden hesaplanıyor (Claude Code JSON objesi verir = tek satır) — iki komut ancak **bütün çıktıları** aynıysa eşleşir; bugün ölçüldü, farklı çıktılı üç deneme ateşlemedi, `ls -la X` / `command ls -la X` / `env ls -la X` ateşledi. (3) **Çıkışı sıfır olmayan Bash çağrısı için PostToolUse olayı hiç gelmedi** (dört exit-127 denemesinde `STEP_START` var, karşılığı yok); `err_sig` yalnız o dalda atanıyor. **YANLIŞ POZİTİF, sayıldı: 1** — hiçbir şeye takılmamıştım, ve metnin "no green move is on record this session" cümlesi de yanlıştı.

**K2 — SESSİZ SKIP: sınıf hakemin saydığından büyük çıktı. 9/8 değil, 14 site / 13 dosya.** Hakemin `grep 'echo "  skip'`'i büyük harfli `SKIP` basan 5 siteyi görmüyordu (`harness_lock`, `heldout`, `usage_order`×2, `wrapper_exec`). Ölçüt önce ve ayrı commit'te (`af0a3b5`, o gün KIRMIZI), düzeltme sonra (`fb7f2be`); arada bir **ölçüt düzeltmesi** var (`eafa3e7`): sayaç echo'nun kendi satırında olmak zorunda değil, yoksa doğru çalışan `harness_lock`+`heldout` haksız suçlu oluyordu — eski/yeni kural yan yana dosyada yazılı.
Kural: dal ya **KIRMIZI düşer** (bayat ağaç: `version`, `guard_lint` — `make_deps` kalıbı) ya da **adıyla ve sayısıyla ilan eder** (`SKIP - <arm>: <n> assertion(s) did NOT run — <why>`, sayı özet satırında). Yeni kilit `native/silent_skip_test.sh`, `make test`'te, kendi mutasyonu içinde (ekilmiş sessiz skip yakalanır, sayılan skip yakalanmaz).
**BULUNAN, ARANMAYAN:** `guard_lint_test.sh`'in ampirik kolu **yazıldığı günden beri hiç koşmamış**. Hedef adını `native/rabadon-` + kök diye tahmin ediyordu, `gate_bench.cpp`'nin hedefi ise `native/gate_bench`; `make -q` "No rule to make target" deyip **exit 2** veriyor, dış `if` bunu "ağaç kurulmamış" okuyup skip'e düşüyordu — **tam kurulu bir ağaçta**. Hedef artık Makefile'dan okunuyor; süit **20 → 22**.
**MUTASYON KANITI 7/9 dalda dıştan zorlandı** (`kanit/f3d/3-skip-mutasyon.out`): bayat ağaç → `version` 13→**11+1 FAIL EXIT=1** (eskiden 11 passed EXIT=0), `guard_lint` 22→20+1 FAIL; `id -u`=0 → `blind_switch` "1 skipped"; python3 gizli → `discovery_scope`; node gizli → `redbase_scope` **9 assertion ilan** (eskiden özet satırı bile basmadan exit 0); caffeinate gizli → `unknown_wrapper` + `wrapper_exec` (6 skip); boş HOME → `usage_order`. **ZORLAYAMADIM: `sandbox` (çekirdek backend yok) ve `script_wrapper` (pty yok)** — bu makinede dıştan üretilemedi, statik yarısı kilitli, **DOĞRULANMADI**.
**BOŞ YEŞİL TURU:** yeni kilit `F3d-oncesi` (`73bda28`) ağacında **KIRMIZI** (3 passed / 1 failed, 15 site / 13 dosya), HEAD'de yeşil. Worktree `/tmp` dışında, `--detach`, **kaldırıldı**.

**K3 — `2b`: BU FAZ DA HIZLANDIRMADI, ve bunu iddia etmiyorum.** `git diff --name-only F3d-oncesi..HEAD` sıcak yolda **tek satır C++ göstermiyor** (yalnız `*_test.sh` + Makefile + docs + kanıt), yani eşli A/B kendi kendini ölçerdi. Tavan **oynatılmadı**, `accept.sh` **hiç değişmedi** (mühürlü dörtlünün diff'i 0 satır).
**Bunun yerine iki sayı ölçtüm, ikisi de sonraki fazın kararını değiştiriyor.**
(a) **Gürültü bandı, A/A ile** (`7-2b-aa.out`, aynı ikili iki kolda, N=500×8, gerçek fark SIFIR): ortalama fark **+107,9 µs**, işaret **6+/2−**, aralık **589,3 µs**, stdev **202,9 µs**. Yani bu makinede eşli bir iddia **|439 µs|**'i aşmalı **ve** tek yönlü olmalı. F3c'nin −23,0 µs / 3-3'ü ve F3b'nin −373,1 µs'lik **büyüklüğü** bu bandın içindedir (F3b'nin **8/8** işareti ayrı ve geçerli bir kanıttır, büyüklüğü değil). Ek olarak: attributable tek koşu içinde **1524 → 2492 µs** diye tek yönlü sürükleniyor — eşlemeden yapılan A-sonra-B karşılaştırması bu makinede sırayla yanlıdır.
(b) **TAVANIN %60'I RABADON İŞ YAPMADAN GİDİYOR** (`8-2b-taban.out`, eşli, kollar sıralı değiştirilmiş, N=400×6): tam yol **1662,0 µs**, `RABADON_OFF=1` (anahtarı oku ve çık) **595,2 µs**, rabadon'un kendi işi **1066,7 µs, 6+/0−** (banda göre gerçek). **1000 µs tavanın 595'i süreç başlangıcı; bütün mantığa kalan pay 405 µs.** `gate.cpp` içindeki hiçbir algoritmik kazanç tek başına tavana ulaşamaz — F3-S1'in sahibi bunu bilerek plan yapmalı.
Bugünün aynı aletle iki okuması: **1482,8** ve **1696,7 µs** (N=500, ikili değişmedi) — aletin gezindiğinin üçüncü kanıtı. Süreç-içi prob (`accept.sh`) **1337,0 µs**.

**KAPI (hepsini kendim koşturdum).** `make test` **MAKE_TEST_EXIT=0**; native **3838** iddia + **633** kontrol + `npm test` **64/0** → **4535 yeşil / 0 kırmızı** (taban 4529, **+6**: `silent skip` 4 + `guard lint` +2). Süit özeti diff'i: **58 → 59**, **düşen ya da küçülen süit YOK**, silinen/zayıflatılan/atlanan test YOK, eşik/tolerans/fikstür/ön-kayıt HİÇ değişmedi. `bash reports/R7/accept.sh` **EXIT=1, 23 yeşil / 3 kırmızı**, ad kümesi **`{2b, 6e, 7b}` — BÜYÜMEDİ**. F1e-C üçlüsü nihai ikiliye karşı **42/0 · 38/0 · 13/0**, kâtip commit'i (`4ee2894`) fazın **son commit'i değil**.
**KAPI BENİ KESTİ, § adıyla:** `no-rm-rf-outside` bir `git worktree remove … ; rm -rf …` bileşiğini reddetti — **doğru retti**. `rm -rf`'i attım, yalnız `git worktree remove --force` kullandım. `guard.json`'a dokunulmadı, `rabadon off` kullanılmadı, CHALLENGE-3 deliği kullanılmadı, `CAP=200` büyütülmedi, korpus ve snapshot'a dokunulmadı.

**NOT VERIFIED / ÖLÇEMEDİM.** `sandbox` ve `script_wrapper`'ın skip dalları dıştan zorlanamadı. İlan edilen "n assertion(s) did NOT run" sayıları **elle yazıldı**, dalın gerçek iddia sayısına karşı hiçbir şey doğrulamıyor (silent_skip'in kendi NOT COVERED notunda yazılı). Temiz konteynerde hiçbir şey koşmadı. Neden exit≠0 Bash çağrısında PostToolUse gelmediği **çözülmedi** (harness mi göndermiyor, gate mi erken dönüyor). (c) negatif kontrol koşulmadı.
