# f1d-A — durum yüzeyi gate'in hesapladığı tek gerçeğe bağlandı

**yapılan** · `native/gate.cpp` (27b84f8): tek `compute_state(dir)` — üç susturucunun HEPSİNİ toplar, katmanlı modu (RABADON_MODE → proje/mode → proje/on → RABADON_DIR/mode → enabled → blind) tek yerde çözer. Sıcak yol KOPYA DEĞİL, aynı fonksiyonu çağırır (eski 50 satırlık ikinci okuma silindi). `--status/--on/--off/--toggle/--silent` ekranı mutasyondan SONRA bu state'ten basılır, `--statusline` lambası da. Ekran `$PWD`'yi getcwd() yerine tercih eder (macOS'ta /tmp → /private/tmp, "rm <yol>" kullanıcının yazmadığı yolu gösteriyordu). `Makefile` (5e77823): `status_truth_test.sh` `test:` reçetesine bağlandı.
**ölçülen** · `make all` → exit 0. `bash native/status_truth_test.sh` → **exit 0, 94 ok / 0 fail** (öncesi 17 ok / 77 fail). `make -i test` → **3615 ok** (`grep -cE '^[[:space:]]*ok\b'`, taban 3504) · **612** (`grep -oE 'PASS \([0-9]+ checks?\)' | grep -oE '[0-9]+' | paste -sd+ - | bc`, taban 612). `npm test` → 64 pass / 0 fail, exit 0. `bash reports/R7/accept.sh` → exit 1, **23 yeşil / 3 kırmızı = {2b, 6e, 7b}** (küme büyümedi); **2b = 1270.3 µs** (tarihsel bant 1184.7–1381.8 µs, regresyon işareti yok). Ekranlar: `reports/kosu/RAPOR/f1d-1-ekran.out`.
**yapılamayan** · `make test` exit 0 DEĞİL — exit 1. Tek kırmızı `native/version_test.sh`: `DRIFT docs/quickstart.md:35,36: a version typed into prose (0.2.3)`. **DEVRALINDI**: `git stash push native/gate.cpp` ile HEAD'de koşturuldu, aynı FAIL (`version: 10 passed, 1 failed`). Kaynak b2 kâtip commit'i 01a0951; `docs/` bu karta yasak, dokunulmadı. Suite'in geri kalanında sıfır kırmızı.
**karttan sapma (kırmızı kilit zorladı, test değiştirilmedi)** · (1) Susturuculu ekranın ilk satırı kartın yazdığı `rabadon: OFF — silenced;…` değil, `rabadon: SILENT — silenced; the gate returns 0 before any rule runs`. Sebep: `status_truth_test.sh:143-150 claim_of()` yalnız ON/WATCH/SILENT okur; `OFF` → `??` → BLOCKED. (2) Kart "tek `next:` satırı" diyor; `check_disclosure` (satır 198) her yürürlükteki susturucu için `^[[:space:]]*next:` satırında O susturucunun komutunu arıyor — iki/üç susturucu varken tek satır matematiksel olarak yetmez, bu yüzden her `silenced by:` altında kendi `next:` satırı var. (3) "ne yaptım" satırında mod adı küçük harf (`` `watch` ``): `claim_of` `*WATCH*`'ı `*SILENT*`'tan ÖNCE arıyor, büyük harf WATCH susturulmuş ekranı "WATCH iddiası" yapıp 0-bayt gate'e karşı kırmızıya düşürürdü.
**PARKED** · `rabadon on` susturucu varken exit 0 döndürmeye devam ediyor (davranış kırılmadı, kart öyle istedi). Ekran modu yazdığını söylüyor ama hiçbir şeyin reddedilmeyeceğini de söylüyor.
**kart dışı fark edilen (DOKUNULMADI)** · `native/regression_demo.sh` çıktısında `FAIL testsuite [node --test]` satırı var — bu suite'in KASITLI demo çıktısı (`regression: 4 passed, 0 failed`), gerçek kırmızı değil; sayaç grep'i yapan biri buna takılabilir. · `--status`'ın `read from:` satırı susturucu varken `(not consulted — a silencer above answers first)` diyor; eski `absent — no file means WATCH` metni "WATCH" kelimesi taşıdığı için susturulmuş ekranda kullanılamazdı.
