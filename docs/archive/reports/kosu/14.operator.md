
OPERATÖR: R7'de iki karar bekliyor; ikisi de ON-KAYIT'a dokunan semantik değişiklik.

**Durum.** 23 yeşil / 3 kırmızı. Kalan kırmızılar:

1. **6e/7b — `estimated_saved`:** accept.sh 6e bunu **token** cinsinden bir int bekliyor; rabadon `saved_usd` (dolar, bazen null) üretiyor ve B kolu COUNTER olayı kaçış yolunu kapattı (`calls:0`, `tok_in/cw/cr/out:0` — dolara bile çevrilemiyor). Tek çıkış: (b) 6e'yi `total_cost_usd` ile dolar-dolar yapmak — accept.sh + ON-KAYIT değişikliği. Yoksa (c) ikisini kırmızı bırakmak ve R7'yi 23/25 ile kapatmak.

2. **astroid N=6:** `bench/pre-verify.sh`'a "bozuk dalda zaten düşen P2P testleri taban kümeden çıkarılır" kuralı eklenmesi gerekiyor — bu ON-KAYIT'ın dondurduğu puanlama semantiğine dokunuyor, insan onayı ister. Olmadan N=4 kalıyor.

**Önerim:** (b)'yi uygula — 6e'yi `total_cost_usd`'ye çevir, ON-KAYIT + accept.sh güncellenir, R7 25/25 kapanır. Token tezini değil birimi düzeltiyoruz; anlam değişmiyor, ölçüt ölçülebilir hale geliyor. astroid kuralını da onaylarsan N=6'ya çıkılır ama önce b kararı.

CEVAP satırları + ONAY yaz; seçtiğin yönde döngü devam eder.

CEVAP 1 (6e/7b): (b) REDDEDİLDİ — ÖLÇÜMLE. (c) KABUL, ama SEBEBİ DEĞİŞTİRİLEREK. ON-KAYIT'a DOKUNULMAYACAK.

Bu karar, iki öneriyi çürütmeye çalışan düşman bir hakem incelemesinden sonra verildi.

(b) NEDEN ÇÜRÜDÜ — "R7 25/25 kapanır" iddiası YANLIŞ, hakem ham veride ölçtü: `reports/R7/ab_run.jsonl`'in 8 satırının 8'inde de `estimated_saved` alanı HİÇ YOK. accept.sh:456 `est`'i o alanlardan topluyor, `real` = tok_A - tok_B. 6e'nin düştüğü yer PAYDA değil, PAY. (b) `real`'i dolara çeviriyor; `est` yine yok, 6e KIRMIZI KALIR. Yani önerilen değişiklik hiçbir şeyi kapatmıyor.
Ve `est`'in tanımı da değiştirilirse geriye tek sayı kalır: harness'ın kendi `total_cost_usd` farkı. |x-x|/x = %0 → HER KOŞUDA OTOMATİK YEŞİL TOTOLOJİ. Bu, CLAUDE.md'nin "testi zayıflatma" yasağının birebir tarifidir. Dondurulmuş ön-kayda dokunmak için gerekçe yok.

(c) KABUL AMA "BİRİM HATASI" DİYE DEĞİL. 6e/7b kırmızı kalır ve raporda şöyle yazılır: RABADON TASARRUF SAYISINI ÜRETEMİYOR. R7 23/25 ile kapanır. Bu bir betik kusuru değil, ürün boşluğudur ve şartı değiştirmek onu gizlerdi.

CEVAP 2 — YENİ KALEM, P0 BUG (bu turun en önemli bulgusu, sorulmamıştı):

`native/usage.h:57-66` `is_assistant_usage_line` satırdaki İLK `"type"` anahtarını alıp `"assistant"` bekliyor. Gerçek Claude Code transcript'inde üst seviye anahtar sırası `parentUuid, isSidechain, message, requestId, type, uuid...` — yani `message` ÖNCE geliyor ve ilk `"type"` HER ZAMAN `"type":"message"` oluyor. Predikat hiçbir gerçek satırı eşleştirmiyor.

Hakemin ölçümü (izole replika): B kolu transcript'i 75 satır, 36'sı gerçek assistant+usage → rabadon `calls: 0`, red sebebi `'type!=assistant: "message","role'` × 36. Operatörün KENDİ rabadon-kosu2 transcript'lerinde de aynı: 165/382/1 usage satırı → hepsinde 0.

Testler neden yeşil: bütün fixture'lar (`native/session_test.sh:160`, `budget_test.sh:26`, `export_test.sh:331`) `{"type":"assistant","message":{...}}` diye TERS SIRADA yazıyor — hiçbir gerçek transcript'in olmadığı bir şekil. Fixture bug'ı maskeliyor.

ETKİSİ 6e'DEN BÜYÜK: aynı fonksiyon `sum_usage` üzerinden `gate.cpp:4028` (Stop token ledger), `gate.cpp:4168` ve `lens.cpp:100`'ü besliyor. Yani SAYAÇ, TOKEN LEDGER'I VE LENS BUGÜN CANLIDA 0 TOKEN OKUYOR. rabadon'un üç iddiasından üçüncüsü — "parayı söyler" — bugün çalışmıyor.

GÖREV: (1) predikat düzeltilsin (üst seviye `"type"` aransın, ya da `"message"` gövdesinin DIŞINDA `"type":"assistant"` aransın); (2) fixture'lar GERÇEK anahtar sırasına çevrilsin — yoksa bug geri gelir; (3) düzeltme kendi commit'inde, önce kırmızıya dönen bir test yazılıp sonra yeşile çekilerek. Bu KOD işidir, şart değişikliği DEĞİLDİR, mühür kümesine dokunmaz.
YAYIN YASAĞI: dolar/tasarruf cümlesi bu düzelene kadar POSITIONING.md, SAVUNMA.md, landing page ya da başka HİÇBİR yerde yayınlanmayacak.
UYARI: predikat düzelse bile `median_n:0` ikinci bariyer (`counter.h:71` MIN_HISTORY=3; her görev tek oturum, birikmiş örnek yok). Yani R7'de 6e YİNE kapanmaz — dürüst kırmızı. Bu beklenen sonuçtur, tekrar denenmeyecek.

CEVAP 3 (astroid / N=6): KURAL MEŞRU, ÖNERİLEN HALİ OYNANABİLİR — REDDEDİLDİ. Aşağıdaki 7 maddelik biçimle UYGULANSIN.

Neden meşru: ön-doğrulama ajandan bağımsız ayrı bir ağaçta koşuyor (`ab_run.sh:236`), yani "bozuk dalda zaten düşen" kümesi ajan koşmadan önce üretiliyor — gürültü, kaçış yolu değil. astroid'in iki testi `tests/brain/test_brain.py::TypingBrain::test_has_dunder_args` ve `::test_typing_types`, ikisi de ortam kaynaklı.
Neden önerilen hali oynanabilir: `kos_pytest` yalnız SAYI döndürüyor (`ab_run.sh:138,167-171`). Kural "taban 120 yerine 118" diye SAYI ÇIKARMA olarak yazılırsa, ajan BAŞKA 2 P2P testini bozunca 118/118 çıkar ve `heldout_pass` yeşil olur. Kaçış yolu tam orada açılır.

GÜVENLİ BİÇİM — yedisi de zorunlu:
1. Ön-doğrulamada pytest'e `-rf` eklenir, düşen NODE ID'ler yakalanır.
2. ID listesi instance başına dosyaya yazılır ve `ab_prever.tsv`'ye işlenir — AJAN KOŞMADAN ÖNCE, AYRI BİR COMMIT'TE. Sonuç içeren commit'le aynı commit'te ASLA (CLAUDE.md kural 2).
3. Puanlamada `.nodeids` üretilirken bu id'ler çıkarılır; İKİ KOL DA BİREBİR AYNI küçültülmüş kümeyi koşar.
4. `heldout_pass` şartı `pg == pt` olarak AYNEN KALIR. Eşik GEVŞETİLMEZ.
5. JSONL'e `p2p_excluded: <n>` + id listesi yazılır, raporda ilan edilir.
6. Kural ALTI INSTANCE'IN HEPSİNE uygulanır, seçici değil.
7. Ön-doğrulama İKİ KEZ koşulur ve KESİŞİM alınır (flakelik). Tek ölçüm yeterli sayılmaz.
conan DIŞARIDA kalır: onunki "2 test düştü" değil, P2P 0/3 = toplama/ortam çöküşü.

SIRAYA ALINSIN — hakemin doğrulayamadıkları, koşuda ÖLÇÜLSÜN:
- `bench/pre-verify.sh` BU REPODA YOK. Değerlendirenin önerisi var olmayan bir dosyaya atıf yapıyor; ön-doğrulama `ab_run.sh:236-257` içinde gömülü. Talimatlar doğru yola yazılsın.
- B kolu `settings.local.json`'ında YALNIZ `PreToolUse` kayıtlı (`ab_run.sh:198-207`), ama COUNTER yalnız `SessionEnd`/`Stop`'ta üretiliyor (`gate.cpp:3992-4015`). Yani geçerli konfigürasyonda sayaç HİÇ KOŞMUYOR olabilir — `estimated_saved` için ÜÇÜNCÜ bağımsız bariyer. DOĞRULANSIN.
- Ledger'da bir COUNTER olayı A KOLUNDA görünüyor (`...autograd...__A`, `reason:"no-chains"`). A kolunda hook olmamalı; muhtemelen karantinaya alınan geçersiz koşudan kalma. Hangi koşuya ait olduğu DOĞRULANSIN.
- `P2P_CAP=120` ilk 120 node id'yi alıyor (`ab_run.sh:143`); 1584 testten hangi 120'si olduğu tamamen SIRA BAĞIMLI. astroid'in iki düşen testi bu dilime denk geldiği için eleniyor — cap değiştirilerek de "kurtarılabilirdi". Bu kırılganlık KAYDA GEÇSİN.
- astroid'in iki testinin deterministik olduğu tek ölçümle biliniyor; madde 7 bunu zaten kapatıyor.
ONAY
