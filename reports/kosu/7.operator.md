
OPERATÖR: `reports/R7/CHALLENGE-2.md` bekliyor — `prev` zincir alanı ve `.head` sidecar'ı normalize edilsin mi? Bu kapanmadan GOAL 3 geçilemez, dolayısıyla R7 kapalı kalamaz.

**Durum:** 11 yeşil / 15 kırmızı. GOAL 3'te tek engel `"prev"` ve `.head`. `prev` = bir önceki satırın **normalize edilmemiş** SHA'sı (`chain.h:191`'de belgelenmiş) — içinde gerçek `ts` var, koşudan koşuya değişiyor, hiçbir uygulama eşleştiremez. `.head` sidecar biçimi `<64hex> <satır sayısı>` — yapan, operatörün yasakladığı `s/^[0-9a-f]{64} /HASH /` deseninin yalnız bunu etkilediğini, ledger JSON satırlarıyla hiç eşleşmediğini kanıtladı.

**Üç seçenek:**

**(a) `prev` ve `.head` normalize edilir:** `kimliksiz()` fonksiyonuna iki satır daha eklenir (JSON `"prev":"..."` alanını siler veya sabitler; `.head` satırını `HASH <N>` yapar). Bu operatörün daha önce izin verdiği üç alana paralel bir genişleme.

**(b) GOAL 3 şartı gevşetilir:** "bayt-bayt aynı" yerine `prev`/`.head` alanları hariç tutularak kıyaslanır — accept.sh'ta yorum satırı + koşul değişikliği.

**(c) Daemon bağımsız belgelenir, GOAL 3'ün "bayt-bayt" şartı kaldırılır:** fail-SAME zaten davranışsal olarak kanıtlandı (çıkış kodu, `ev`, `mode`, `rule`, ret metni, `seq` aynı); `prev` bir iç zincir alanı, dış API'nin parçası değil.

Önerim **(a)**: küçük, izole, veriye dayalı. `prev`'in normalize edilmesi önceki kararla aynı tür bir izin — kimlik alanı, değer alanı değil. Kod değil, sadece `accept.sh`'taki `kimliksiz()` genişler. CEVAP: satırlarını yaz, en sona tek başına ONAY yaz.

CEVAP: (d) — (a) KABUL, AMA TEK BAŞINA DEĞİL. accept.sh'ın GOAL 3'üne bir ZORUNLU KONTROL EKLENİR. (b) ve (c) REDDEDİLDİ.

Bu karar düşman bir hakem ajanın iddiayı çürütmeye çalışmasından sonra verildi. İddia DOĞRULANDI, ama yapanın atladığı iki şey var.

DOĞRULANAN: native/chain.h:200-204 — `.head`'e yazılan `rbsha::hex(chained)` HAM satırın SHA'sı; bir sonraki çağrıda chain.h:198 `read_head()` ile okunup o satırın `prev`'i oluyor. Hakem /tmp'de izole iki koşu üretti: `sha256(satır1) == satır2.prev` True, satır1'in ham içeriğinde `"ts":1787570278293` vs `...308`. Yani prev koşudan koşuya kaçınılmaz değişiyor; hiçbir dürüst uygulama eşleştiremez. Zamanın geçmemesini isteyen bir şart test değil, çelişkidir.

YAPANIN ATLADIĞI 1: `echo hello world` (izin yolu) TEK satır yazıyor, `prev:"genesis"`. O komutta ledger satırı zaten birebir aynı — tek fark `.head`. Yani üç komutun hepsinde suçlu `prev` değil; birinde yalnız sidecar.

YAPANIN ATLADIĞI 2 (ve tehlikeli olan): "zincir normalize edilmiş girdiden hesaplansaydı deterministik olurdu" yolu. Hakem ölçtü, gerçekten deterministik oluyor (`sha256(normalize(satır1))` iki koşuda da 22691cec92f8e8e3...). BU YOL KESİNLİKLE YASAK: o zaman zincir ts/run/yol alanlarına TAAHHÜT ETMEZ, saldırgan zaman damgasını serbestçe değiştirir ve zincir SAĞLAM görünür. Bir testi geçirmek için üründeki kurcalama kanıtını yok etmek olurdu. Gelecekte bu öneri gelirse REDDEDİLECEK.

NE KAYBEDİYORUZ — ölçüldü: prev körleştirildiğinde iki koşunun normalize satırları TAMAMEN aynı; prev tek başına artık-fark. prev'in taşıdığı davranış sinyali (ev/mode/rule) zaten satır gövdelerinde bayt bayt karşılaştırılıyor, yani %100 yedekli. Görünmez olan TEK şey zincirin BAĞLANTI ARİTMETİĞİ: prev doğru hesaplanıp YANLIŞ satırdan alınsa GOAL 3 göremezdi. Bu boşluk kapatılacak — aşağıdaki 3. madde.

İZİN VERİLEN DEĞİŞİKLİK — accept.sh KENDİ COMMIT'İNDE, kod olmadan:
1. `kimliksiz()`'e `s/"prev":"[0-9a-f]{64}"/"prev":"PREV"/g` eklenir. YALNIZ 64-hex körleşir: `genesis` ve EKSİK prev ayırt edilebilir kalır (prev'in tamamen kaybolması hâlâ fark olarak görünmeli — bu gerçek bir sinyal).
2. `.head`'de yalnız baştaki hash körleşir (`s/^[0-9a-f]{64} /HASH /`), SAYAÇ OLDUĞU GİBİ KALIR. Yapanın "bu desen ledger JSON satırlarıyla hiç eşleşmiyor, yalnız sidecar'ı etkiliyor" kanıtı doğrulandı; önceki kararımdaki yasak bu dar kullanım için KALDIRILDI, çünkü satır sayısı korunuyor.
3. ZORUNLU EK — bu madde olmadan 1 ve 2 GEÇERSİZDİR: GOAL 3'ün HER İKİ KOLUNDA, karşılaştırmadan ÖNCE `native/rabadon-audit` o kolun spool'una karşı koşulur ve `INTACT` + exit 0 ŞART KOŞULUR. Böylece koşular ARASINDA hiçbir dürüst uygulamanın sağlayamayacağı bir hash eşitliği aramayı bırakıp, her koşunun İÇİNDE zincirin gerçekten sağlam olduğunu doğruluyoruz. Kaybedilen tek sinyal (bağlantı aritmetiği) daha keskin biçimde geri geliyor. Bu bir test ZAYIFLATMA değil, test GÜÇLENDİRME hamlesidir ve izin bu şarta bağlıdır.

(b) ve (c) REDDEDİLDİ: bayt-bayt şartı doğru ve sinyalin çoğu orada; gevşetmeye gerek yok. Ayrıca kurcalama kanıtı zaten R7 GOAL 3'e bağlı değil — `native/audit_test.sh` (Makefile:107 ile suite'te) 8 senaryoyla gerçek gate spool'u üzerinde test ediyor (dokunulmamış → INTACT; tek karakter → "chain BROKEN at line 3"; satır silme; tüm prev'lerin sıyrılması; gün dosyası silme; zincirin yeniden dikilmesi → .head sayacı yakalıyor). GOAL 3 bir fail-SAME determinizm testidir, tamper-evidence testi değil.

SIRAYA ALINSIN — hakemin sorulmayan bulguları:
- GOAL 2b 1583-1704 µs, tavanın ~1.6 katı, KIRMIZI. En güçlü şüpheli gated.cpp:19-24'te BELGELENMİŞ "istek başına İKİ fork". Henüz ÖLÇÜLMEDİ — 1704 µs bileşenlerine ayrılsın (istemci-öncesi / IPC / worker yargılaması). Bu ayrıştırma gelmeden hız iddiası hakkında YENİ BİR KARAR VERİLMEYECEK.
- 2c YEŞİL ama sayısı GÜVENİLMEZ: sapma dakikalar arayla %4.98 → %1.92, gürültü içinde. "Ölçüldü" denemez; LENGTH.md'ye bu belirsizlik yazılsın.
- native/gate.cpp:720 open_sock() sessiz strncpy kesmesi HÂLÂ AÇIK (Promise 1 ihlali). İki turdur taşınıyor.
- GOAL 4-7'nin kanıt kolu hâlâ hiç incelenmedi.
ONAY
