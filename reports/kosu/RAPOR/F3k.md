# F3k — kart

**3 KART. TAHMİN (§3.12, koddan önce, `3ee5023` 12:55):** K1 90 dk · K2 120 dk · K3 30 dk = **240 dk** (2 katı 480). Gerçekleşen **~40 dk**, limite takılmadım.

**KAPI:** `make test` **EXIT=0** · native **4096** + `PASS` **633** + npm **64/0** = **4793 yeşil / 0 kırmızı** (taban 4783, **+10**) · süit **121** (değişmedi) · sıfırdan büyük `failed` **0** · `skip` **0**. `accept.sh` **EXIT=1, 23/3, `{2b, 6e, 7b}` BÜYÜMEDİ**. **Küçülen süit 0, kaybolan süit 0** — değişen tek süit `selfheal_path` **21 → 31**. F1e-C üçlüsü nihai ikiliye karşı **42/38/13, üçü de EXIT=0**. Mühürlü set + `bin/rabadon.mjs` (O3) diff'te **YOK**; ürün diff'i **yalnız `hooks/refresh.mjs`**.

**K1 — `/private/tmp` deliği KAPANDI, ve şekil değil AİLE olarak.** `notDurable` üç kola ayrıldı: (1) adlandırılmış kökler (`os.tmpdir`/`TMPDIR`/`TMP`/`TEMP`), (2) **yapısal** kol — `realpath` sonrası her ata için *world-writable + sticky* (1777), (3) worktree. Ölçü artık dizge değil, dosya sisteminin kendi işareti. **Üçü de ürünün listesinde OLMAYAN kök yakalandı:** `/private/tmp`, `/private/var/tmp`, ve süidin `$HOME` altında 1777 ile **kendi kurduğu, adı hiçbir kaynak dosyada geçmeyen** kök. Gizil kalem de kapandı: kol 3 artık `PKG_DIR`'i değil **parametresini** soruyor (en yakın `.git` FILE mı?), `PKG_DIR` ölü değişken olarak kalktı. Her repoint ilan ediliyor (Promise 1) — üç kökün üçünde de "will NOT repoint" ekranda.
**Kapı gerçek:** ölçüt commit'i (`2935489`) koddan AYRI ve ÖNCE; oradaki `refresh.mjs` **`F3k-oncesi` ile bayt bayt aynı** (`git diff` 0 satır) ve süit orada **KIRMIZI: 25/6, EXIT=1** — boş yeşil turu tabanda kırmızı. **Mutasyon:** kol 2 devre dışı → **31/0 → 25/6 (EXIT=1)** → geri alındı → **31/0**, `git diff` boş. **Aşırı-uyum kilidi:** "durable-install" kolu kalıcı bir kopyanın **hâlâ kabul edildiğini** ölçüyor, yani kapı iki yönde de kırılabilir — "her şeyi reddet" bu iki iddiada kırmızı düşer.

**K2 — DÜŞÜREMEDİM. İLAN EDİYORUM: daemon HÂLÂ +2645,3 µs (3 çift) ve +3365,9 µs (4 çift) YAVAŞ**, **7 çiftin 7'si** yavaş yönde, bant **|439| µs** — farkın 6-8 katı. Eşli/ABBA, tek oturum, N=200, zorunlu alet `2b-uctan-uca.sh` değiştirilmeden. **Kol ayrımı kanıtlandı:** gate'in BLOKLADIĞI olay soket kolunda 0, süreç-içi kolda 2.
⚠ **KARTIN MEKANİZMASI ARTIK YOK:** `gate.cpp:551-591`'de `rb_day_str` **gmtime_r çağırmıyor** (Hinnant tamsayı takvimi + güne anahtarlı önbellek). 269-483 µs'lik soğuk timezone yükü sevk edilen koddan **zaten kalkmış**; `:538`'deki cümle o önbelleğin neden var olduğunu anlatan geçmiş zaman. Yani gecikmenin sebebi timezone **değil**.
**Sebep ölçüldü:** sıfır-fork/sıfır-yargı bir ölçüm aletiyle (`k2-null-daemon.c`, ürün değil, kurulmaz) **TABAN = 1450,7 µs vs sevk edilen A = 3494,1 µs → IPC 2043 µs daha UCUZ**. Yani soket gidiş-dönüşü sorun değil; ceza tamamen **2 fork + işçinin yargıyı baştan yapması**. Asıl yanılgı `gated.cpp:11-17`'nin kendi gerekçesinde: "işçi 2,3 ms'i zaten ödemiş bir süreçten fork ediliyor" — **sevk edilen yolda geçersiz**, çünkü çağıran `rabadon-gate`'in kendisi ve o 2,3 ms'i daemon'a sormadan önce zaten ödüyor. Daemon istemcinin başlangıcından hiçbir şey kurtarmıyor, üstüne IPC+2 fork+aynı yargı ekliyor.
**waitpid'e, `gated.cpp`'ye, `accept.sh`'e, 1000 µs tavanına DOKUNMADIM.** `2b`'nin iki şıkkından birini seçmek kapı tanımını değiştirmektir (§3.8/1) → **HAKEME BIRAKIYORUM**.

**K3 — ATFEDİLDİ, "ölçemedim" demeye gerek kalmadı.** Aynı tek logda: **GENİŞ** `grep -cE '^[[:space:]]*ok\b'` = **4086** (= kartın sayısı, `DURUM.md:752` TEK GEÇERLİ SAYAÇ) · **DAR** `grep -c '^  ok'` = **4034** (= hakemin sayısı, `DURUM.md:739` **EMEKLİ**). Fark **52 = 38 + 14**, `ok`'u **sütun 0'dan** basan iki süit (`usage --signals`, `lens`). **Kayıp iddia yok, ortam/sıra etkisi yok — hakem emekli regex'i kullanmış.** `DURUM.md`'nin üç komutu değişmedi, CHALLENGE yok.

**SAHİBİNİN MAKİNESİ — DÖRDÜ DE ÖLÇÜLDÜ, SAĞLAM:** `~/.claude/settings.json` **`adcb41a9…`** oturum başında, her mutasyonun ve her `make test`'in çevresinde, sonunda **AYNI**; 17 hook, **0 kırık rabadon yolu** · **20 ikilinin 20'si bayt bayt aynı**, hepsi `-rwxr-xr-x` · fren `mode=watch`, `enabled` **YOK** (sahibinin kalemi, dokunmadım) · **artık klasör yok**: `$HOME`, `/private/tmp`, `/private/var/tmp` temiz, başıboş daemon yok, **`git worktree list` tek satır — worktree AÇMADIM**. Sahibinin ölü `/tmp/rabadon-501.sock`'una **dokunmadım** (ölçümde her iki kolu da özel `XDG_RUNTIME_DIR`'e alarak dışarıda bıraktım). `guard.json`'a dokunmadım · `rabadon off` kullanmadım · CHALLENGE-3 deliğini kullanmadım.

**§4.3 — YANLIŞ POZİTİF ADAYI: 0.** Kapı bu oturumda beni hiç kesmedi; mod `watch` olduğu için bu, kapının enforce altında çalıştığının kanıtı **DEĞİLDİR** ve öyle yazmıyorum.

**ÖLÇEMEDİKLERİM / DOĞRULANMADI:** (1) **Tek-fork'lu daemon YAZMADIM** — "bir fork kaldırılsa da ~1000-1900 µs yavaş kalır" cümlesi gürültülü ortalamalar üstünde **TÜRETİLMİŞTİR, ölçülmemiştir**; fork başına ~1300-1700 µs tahmini de öyle. (2) Makine yüklüydü: A kolu 1675-12740 µs okudu — **güvendiğim şey büyüklük değil, 7/7 işaret**. (3) Ölü `/tmp/rabadon-501.sock`'un µs maliyetini yine ölçmedim (kapsam dışı bıraktım). (4) `machine_intact`/`selfheal_path` **yalnız macOS'ta** koşuldu; sticky-bit kolunun Linux/konteyner davranışı (`/dev/shm`, refenv) **DOĞRULANMADI** — süit orada daha az kök bulur ve `N_SCRATCH>=2` iddiası bunu sessiz atlamaya değil **kırmızıya** çevirir. (5) `2b`'nin bu turdaki 3616,2 µs'i F3j'nin 2009,0'ından yüksek; **ürün diff'im hot path'e dokunmuyor** (yalnız SessionStart'taki self-heal), farkı makine yüküne bağlıyorum ama **izole etmedim**.

**KAPSAM DIŞI, DOKUNMADIM:** (c) negatif kontrolü (F6'nın aleti; **F4 (c) ölçülmeden açılmaz**) · yasa ailesinin 21 açık şekli (eşitlik kilidi 21=21 tutuyor) · fren.
