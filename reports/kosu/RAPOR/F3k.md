# F3k — kart

**3 KART. TAHMİN (§3.12, koddan önce yazıldı):**
- K1 `notDurable` kalıcılık ailesi (`/tmp`, `/private/tmp`, `$TMPDIR`, worktree, listelenmemiş üç kök) — **90 dk**
- K2 `rabadon-gated` istek başına `fork()` — düşür ya da "hâlâ N µs yavaş" diye ölç ve ilan et — **120 dk**
- K3 mutlak native sayacının 52'lik farkını **atfet** ya da "ölçemedim" yaz — **30 dk**

**Toplam tahmin: 240 dk. İki katı = 480 dk; aşarsam dururum ve hakeme giderim.**

(Kart gövdesi faz sonunda buraya yazılır.)
