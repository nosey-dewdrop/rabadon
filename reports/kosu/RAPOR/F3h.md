# F3h — kart sayısı 4, tahminler (§3.12, koddan ÖNCE yazıldı)

| kart | iş | tahmin |
|---|---|---|
| **K0** | `make test` canlı `~/.claude/settings.json`'ı eziyor: hangi süit yazıyor, neden, nasıl izole edilir | **90 dk** |
| **K1** | `baseline-law-unmade` beş yanlış pozitifi — daraltarak kapat, twin-arm fikstür, `docs/guard.md` ölçümle düzelt | **120 dk** |
| **K2** | Aile: `mv .` / `tar -x` / glob / yorumlayıcı + listede olmayan ≥3 şekil; çıkış kapısı belgeli | **120 dk** |
| **K3** | Açıklanmayan 911 µs'yi ÖLÇ (profil), sonra `2b` hakkında konuş | **90 dk** |

Toplam tahmin **7 saat**; iki katı = 14 saat, aşılırsa dur ve hakeme git.
